---
phase: 02-analysis-contracts-versioned-detection
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src-tauri/src/analysis/service.rs
  - src-tauri/src/clipboard/content_detector.rs
  - src-tauri/src/clipboard/monitor.rs
  - src-tauri/src/capture/runtime.rs
  - src-tauri/src/integration_tests.rs
autonomous: true
requirements:
  - DETE-01
  - DETE-02
  - DETE-04
must_haves:
  truths:
    - 'capture path 在 Phase 1 的 marker-first gating 之后，只调用一个 Rust authoritative analysis service，不再直接把 tuple subtype 写到 entry 上。'
    - 'supported subtype 的 typed metadata 在 capture-time 就已经固化进 `AnalysisSnapshot`，而不是依赖前端二次推断。'
    - '当 analysis 失败、extractor 退化或 metadata 无法生成时，raw content 仍会入库，同时 companion row 记录 `fallback` status 与 diagnostics。'
  artifacts:
    - path: 'src-tauri/src/analysis/service.rs'
      provides: 'capture-time authoritative analysis orchestration 与 fallback diagnostics'
    - path: 'src-tauri/src/clipboard/monitor.rs'
      provides: 'marker-first gating 之后的 analysis service 接入'
    - path: 'src-tauri/src/capture/runtime.rs'
      provides: 'raw row + entry_analysis 同步持久化与 emitted joined entry'
    - path: 'src-tauri/src/integration_tests.rs'
      provides: 'capture-time analysis/fallback integration tests'
  key_links:
    - from: 'src-tauri/src/clipboard/monitor.rs'
      to: 'src-tauri/src/analysis/service.rs'
      via: 'text capture analysis'
      pattern: 'TextAnalysisService'
    - from: 'src-tauri/src/capture/runtime.rs'
      to: 'src-tauri/src/analysis/repository.rs'
      via: 'same save flow persists companion row'
      pattern: 'upsert_entry_analysis'
    - from: 'src-tauri/src/integration_tests.rs'
      to: 'src-tauri/src/capture/runtime.rs'
      via: 'persisted history assertions'
      pattern: 'analysis'
---

<objective>
把 authoritative analysis service 真正接到 capture/runtime 链路上，并实现 explicit graceful fallback：原始内容永远可看，失败诊断永远可追。

Purpose: 这一层完成后，Phase 2 的语义权威就从“定义好了 contract”升级为“真实 capture path 已经按 contract 运行”，同时不破坏 Phase 1 的 marker-first 边界。
Output: capture-time authoritative analyzer、fallback diagnostics、runtime companion persistence，以及集成级自动化验证。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-03-SUMMARY.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-05-SUMMARY.md
@src-tauri/src/analysis/contract.rs
@src-tauri/src/clipboard/content_detector.rs
@src-tauri/src/clipboard/monitor.rs
@src-tauri/src/capture/runtime.rs
@src-tauri/src/integration_tests.rs

<interfaces>
Phase 1 已经建立的 capture 边界：

```rust
// monitor 只在 Persist disposition 下继续做文本检测和 tx.send(entry)
process_text_capture_with_detector(..., detector: F) -> Result<Option<CaptureDisposition>>
```

当前 brownfield 仍在直接写 legacy 字段：

```rust
let (subtype, metadata) = detector(trimmed_text);
entry.content_subtype = Some(subtype_str);
entry.metadata = metadata_json;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 authoritative analyzer，输出 subtype-specific metadata 与 explicit fallback diagnostics</name>
  <files>src-tauri/src/analysis/service.rs, src-tauri/src/clipboard/content_detector.rs, src-tauri/src/integration_tests.rs</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md, src-tauri/src/analysis/contract.rs, src-tauri/src/analysis/service.rs, src-tauri/src/clipboard/content_detector.rs, src-tauri/src/integration_tests.rs</read_first>
  <behavior>
    - Test 1: stable supported subtype corpus 仍保持 URL、JSON、code、command、color、markdown、email、IP、timestamp、base64、plain_text 的 deterministic 结果。
    - Test 2: subtype-specific metadata 至少覆盖 URL parts、颜色格式、detected language、timestamp formats、command name / shell hint、email domain、IP version、markdown hints、base64 sizes。
    - Test 3: extractor 失败或 metadata 无法生成时，service 返回 `status = fallback`、`subtype = plain_text`，并附带 diagnostics，而不是沉默地变成普通 plain text。
  </behavior>
  <action>在 `TextAnalysisService` 中集中维护 precedence 和 typed metadata 提取，不再把“语义是啥”留给 React 推断。可以继续复用当前 `ContentDetector` 的原子判断能力，但要把输出提升为 `AnalysisSnapshot`：为 email / IP / markdown / command 等当前缺 metadata 的 subtype 补稳定字段；对 JSON、Base64、timestamp、颜色、URL 的 metadata 也统一映射到 `AnalysisMetadata` variant。遇到 parse / decode / extraction 失败时，不要抛弃 raw text，不要写 `None` 然后当作真 `plain_text`；必须构造 `fallback_plain_text(...)`，写上 reason code、message 和保留的 raw preview 线索。测试名至少包含 `test_content_detector_supported_subtypes_are_stable`，并新增 service 级 fallback diagnostics test。</action>
  <acceptance_criteria>
    - `rg -n "fallback_plain_text|AnalysisDiagnostic|AnalysisStatus::Fallback|status: AnalysisStatus::Fallback" src-tauri/src/analysis/service.rs src-tauri/src/clipboard/content_detector.rs` 有匹配结果。
    - `rg -n "command_name|shell_family|domain|version|has_heading|has_list|has_code_fence|root_kind" src-tauri/src/analysis/service.rs src-tauri/src/analysis/contract.rs` 有匹配结果。
    - `rg -n "test_content_detector_supported_subtypes_are_stable|fallback" src-tauri/src/integration_tests.rs src-tauri/src/clipboard/content_detector.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_content_detector_supported_subtypes_are_stable -- --nocapture && cargo test test_text_analysis_service_fallback_preserves_diagnostics -- --nocapture</automated>
  </verify>
  <done>authoritative analyzer 已经能给出稳定 subtype + typed metadata，并能明确区分真实 `plain_text` 与 analysis fallback。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 monitor/runtime，让 raw entry 与 companion analysis 在同一保存链路内落地</name>
  <files>src-tauri/src/analysis/service.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/capture/runtime.rs, src-tauri/src/integration_tests.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-05-SUMMARY.md, src-tauri/src/clipboard/monitor.rs, src-tauri/src/capture/runtime.rs, src-tauri/src/analysis/repository.rs, src-tauri/src/integration_tests.rs</read_first>
  <behavior>
    - Test 1: `Persist` 的 text capture 会通过 authoritative analyzer 生成 snapshot，并与 raw entry 一起持久化。
    - Test 2: analyzer fallback 时，raw content 仍写入 `clipboard_entries`，同时 companion row 记录 `fallback` 与 diagnostics。
    - Test 3: `CurrentOnly` / `Skip` 事件仍不会进入 analysis / persistence 链路，保持 Phase 1 capture gating 约束。
  </behavior>
  <action>把 `src-tauri/src/clipboard/monitor.rs` 里的 `process_text_capture_with_detector(...)` 改成消费 `TextAnalysisService`，在 Phase 1 的 `Persist` gating 之后生成 `AnalysisSnapshot`，并把它挂到 entry 载体上送入 runtime。`CaptureRuntime` 的 save flow 需要在同一次保存链路里写 raw `clipboard_entries` 和 `entry_analysis` companion row；若同 hash 重复出现，则 raw row 继续走既有 UPSERT/`copy_count` 合同，analysis row 走 version-aware UPSERT。runtime 最终发给前端的事件 payload 应该是 join 后的 entry，确保 `entry.analysis` 与兼容字段同时可用。保持 marker-first boundary：`CurrentOnly` / `Skip` 不做 analysis、不写 companion row，也不破坏 observed-hash dedupe。</action>
  <acceptance_criteria>
    - `rg -n "TextAnalysisService|AnalysisSnapshot" src-tauri/src/clipboard/monitor.rs src-tauri/src/capture/runtime.rs` 有匹配结果。
    - `rg -n "upsert_entry_analysis|entry.analysis|analysis.status" src-tauri/src/capture/runtime.rs src-tauri/src/integration_tests.rs` 有匹配结果。
    - `rg -n "CurrentOnly|Skip" src-tauri/src/clipboard/monitor.rs` 仍有匹配结果，证明没有绕过 Phase 1 gating。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_capture_runtime_failure_analysis_falls_back_to_plain_text -- --nocapture && cargo test test_integration_text_entries_store_analysis_contract -- --nocapture</automated>
  </verify>
  <done>capture-time authoritative analysis、graceful fallback 和 companion persistence 已真正接入运行链路，Phase 2 的后续工作可以直接消费 persisted analysis。</done>
</task>

</tasks>

<verification>
先确认 analyzer 本身的 subtype / fallback 行为，再确认 monitor/runtime 集成后 raw entry 与 companion row 同步落地。两步都通过，才算 DETE-01/02/04 的后端主链路闭合。
</verification>

<success_criteria>
所有 `Persist` 的 text capture 都由 Rust authoritative analyzer 产生 `AnalysisSnapshot`，并在失败时保留 raw content + diagnostics，而不是把失败静默吞成普通 `plain_text`。
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-contracts-versioned-detection/02-03-SUMMARY.md`
</output>
