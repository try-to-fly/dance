---
phase: 02-analysis-contracts-versioned-detection
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/lib/preview/entryPresentation.ts
  - src/lib/preview/previewDescriptor.ts
  - src/stores/clipboardStore.ts
  - src/components/DetailView/DetailView.tsx
  - src/components/DetailView/DetailView.test.tsx
  - src/components/DetailView/DetailPreviewContract.test.tsx
autonomous: true
requirements:
  - DETE-01
  - DETE-02
  - DETE-04
must_haves:
  truths:
    - '前端详情、preview descriptor 和 store 只消费 authoritative `entry.analysis` 或明确的 legacy fallback，不再自己重新发明 subtype 语义。'
    - 'degraded / fallback analysis 的条目仍能查看 raw content，并能看到 status 或 diagnostics 提示。'
    - 'URL / Base64 的 preview resolution 仍可继续做渲染降级，但不会再反向决定内容 subtype。'
  artifacts:
    - path: 'src/lib/preview/entryPresentation.ts'
      provides: 'analysis-first subtype / metadata / diagnostics 访问 helper'
    - path: 'src/lib/preview/previewDescriptor.ts'
      provides: 'detail preview 的 minimal authoritative analysis consumption'
    - path: 'src/stores/clipboardStore.ts'
      provides: '只做 preview resolution、不再做 subtype inference 的前端 store'
    - path: 'src/components/DetailView/DetailPreviewContract.test.tsx'
      provides: 'detail contract tests 覆盖 analysis-first 与 degraded fallback 行为'
  key_links:
    - from: 'src/lib/preview/entryPresentation.ts'
      to: 'src/types/clipboard.ts'
      via: 'analysis snapshot helpers'
      pattern: 'analysis'
    - from: 'src/stores/clipboardStore.ts'
      to: 'src/components/DetailView/DetailView.tsx'
      via: 'resolveEntryPreview keeps rendering data separate from subtype authority'
      pattern: 'resolveEntryPreview'
    - from: 'src/lib/preview/previewDescriptor.ts'
      to: 'src/components/DetailView/DetailPreviewContract.test.tsx'
      via: 'contract tests'
      pattern: 'buildPreviewDescriptor'
---

<objective>
以前端最小变更消费 authoritative analysis：detail/presentation 认 `entry.analysis`，store 停止继续推断 subtype，degraded 条目仍保持 raw-first 可见。

Purpose: Phase 2 的目标不是重写预览系统，而是把“谁定义 subtype 和 metadata”这件事收回 Rust，并让当前 detail 流程以最小成本尊重这个权威来源。
Output: analysis-first 的前端 helper、preview descriptor、detail view / tests，以及去掉 subtype 推断的 store。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md
@src/types/clipboard.ts
@src/lib/preview/entryPresentation.ts
@src/lib/preview/previewDescriptor.ts
@src/stores/clipboardStore.ts
@src/components/DetailView/DetailView.tsx
@src/components/DetailView/DetailPreviewContract.test.tsx

<interfaces>
当前前端还在自己重新推断语义：

```ts
const inferTextSubtypeFromUrl = (url: string): ContentSubType => { ... }
const subType = (entry.content_subtype || 'plain_text') as ContentSubType;
```

当前 descriptor 还是基于 legacy fields：

```ts
const metadata = parseContentMetadata(entry.metadata);
const subType = getEntrySubType(entry);
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 让 entryPresentation / previewDescriptor 改成 analysis-first，并暴露 fallback diagnostics</name>
  <files>src/lib/preview/entryPresentation.ts, src/lib/preview/previewDescriptor.ts, src/components/DetailView/DetailPreviewContract.test.tsx</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md, src/types/clipboard.ts, src/lib/preview/entryPresentation.ts, src/lib/preview/previewDescriptor.ts, src/components/DetailView/DetailPreviewContract.test.tsx</read_first>
  <action>在 `entryPresentation.ts` 中新增 analysis-first helper，例如 `getEntryAnalysis(entry)`、`getEntryAnalysisSubtype(entry)`、`getEntryAnalysisMetadata(entry)`、`getEntryAnalysisDiagnostics(entry)`；规则固定为：优先使用 `entry.analysis`，只有当 companion row 尚未 backfill 时才回退 `content_subtype` / `metadata`。`previewDescriptor.ts` 也按同一规则取 subtype 和 metadata，并在 `analysis.status === 'fallback'` 时增加最小可见的 degraded badge / inspector section，把 diagnostics 暴露给 detail view；但不要在本计划引入新的复杂 renderer，也不要扩展到搜索/分享等超 scope 需求。同步更新 `DetailPreviewContract.test.tsx`，加入“analysis-first override legacy fields”和“fallback 条目仍保留 raw 主视图与 diagnostics inspector”两类 contract tests。</action>
  <acceptance_criteria>
    - `rg -n "getEntryAnalysis|analysis.status|getEntryAnalysisDiagnostics|legacy fallback" src/lib/preview/entryPresentation.ts src/lib/preview/previewDescriptor.ts` 有匹配结果。
    - `rg -n "fallback|diagnostic|analysis-first" src/components/DetailView/DetailPreviewContract.test.tsx` 有匹配结果。
    - `rg -n "parseContentMetadata\\(entry.metadata\\)" src/lib/preview/previewDescriptor.ts` 不再是唯一 metadata 来源，文件中应有 `entry.analysis` 的优先消费逻辑。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/DetailView/DetailPreviewContract.test.tsx</automated>
  </verify>
  <done>detail preview 的 helper 与 descriptor 已以 `entry.analysis` 为第一权威，并能把 fallback diagnostics 暴露给上层 UI。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收紧 store / DetailView，停止前端 subtype inference，只保留 rendering fallback</name>
  <files>src/stores/clipboardStore.ts, src/components/DetailView/DetailView.tsx, src/components/DetailView/DetailView.test.tsx, src/components/DetailView/DetailPreviewContract.test.tsx</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md, src/stores/clipboardStore.ts, src/components/DetailView/DetailView.tsx, src/components/DetailView/DetailView.test.tsx, src/components/DetailView/DetailPreviewContract.test.tsx</read_first>
  <behavior>
    - Test 1: store 只根据 authoritative subtype 决定是否走 URL / Base64 preview resolution，不再从 URL 后缀推断 `json` / `markdown` / `command` 等语义。
    - Test 2: degraded / fallback analysis 的 detail view 仍渲染 raw content，并展示最小 diagnostics 或 status 提示。
    - Test 3: companion row 缺失的 legacy entry 仍可正常显示，不会因为 `analysis = null` 而崩溃。
  </behavior>
  <action>收敛 `src/stores/clipboardStore.ts`：删除或停用 `inferTextSubtypeFromUrl(...)` 这类会反向定义 subtype 的逻辑，保留 URL/Base64 preview fallback 仅用于决定渲染数据或 `previewKind`，不要再改变 entry 的语义 subtype。`resolveEntryPreview()` 必须先读 authoritative subtype，再决定是否调用 `resolveUrlPreview()` / `decodeBase64Preview()`。`DetailView.tsx` 用新的 analysis-first helper 构建标签、描述和 degraded 展示，不因为 fallback 条目而隐藏 raw content。同步在 `DetailView.test.tsx` 和 `DetailPreviewContract.test.tsx` 增加 regression tests，确保 analysis 缺席与 degraded 两种场景都被覆盖。</action>
  <acceptance_criteria>
    - `rg -n "inferTextSubtypeFromUrl" src/stores/clipboardStore.ts` 无匹配结果，或函数已彻底不再参与 subtype authority。
    - `rg -n "entry.analysis|analysis.status|diagnostics" src/components/DetailView/DetailView.tsx src/components/DetailView/DetailView.test.tsx` 有匹配结果。
    - `rg -n "resolveEntryPreview|resolveUrlPreview|decodeBase64Preview" src/stores/clipboardStore.ts` 仍有匹配结果，证明只做 rendering fallback，没有删除 Phase 3 仍会需要的预览能力。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/DetailView/DetailPreviewContract.test.tsx src/components/DetailView/DetailView.test.tsx</automated>
  </verify>
  <done>前端已经停止自创 subtype 语义，只消费 Rust authoritative analysis，并在 degraded 情况下保持 raw-first 的用户可见性。</done>
</task>

</tasks>

<verification>
先通过 preview contract tests 锁定 analysis-first helper，再通过 DetailView tests 确认 degraded / legacy fallback 行为对用户可见且不回归。
</verification>

<success_criteria>
前端 detail/preview/store 已经最小化地尊重 Rust authoritative analysis：语义权威不再来自 React，degraded 条目依旧能查看 raw content 和 diagnostics。
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-contracts-versioned-detection/02-04-SUMMARY.md`
</output>
