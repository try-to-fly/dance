---
phase: 02-analysis-contracts-versioned-detection
plan: 07
type: execute
wave: 6
depends_on:
  - 02-06
files_modified:
  - src/components/DetailView/scene/DetailScene.tsx
  - src/components/DetailView/scene/AlternateViews.tsx
  - src/components/DetailView/scene/AlternateViews.test.tsx
  - src/components/DetailView/ContentRenderers/JsonRenderer.tsx
  - src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx
  - src/components/DetailView/DetailView.test.tsx
autonomous: true
requirements:
  - DETE-04
gap_closure: true
must_haves:
  truths:
    - 'JSON 条目即使只有 raw 一个备用视图，用户也仍然能在 detail 中切换查看原文。'
    - 'detail 左列在长内容场景下具备稳定的纵向滚动能力，主预览和下方备用视图不会再被共同祖先裁掉。'
    - 'JsonRenderer 的代码视图拥有明确高度，切换后不会再出现 Monaco 空白。'
  artifacts:
    - path: 'src/components/DetailView/scene/DetailScene.tsx'
      provides: 'non-immersive detail column scroll container 与 raw-only alternate visibility contract'
    - path: 'src/components/DetailView/scene/AlternateViews.tsx'
      provides: 'single raw alternate view 渲染路径'
    - path: 'src/components/DetailView/ContentRenderers/JsonRenderer.tsx'
      provides: 'explicit-height JSON tree/code renderer layout'
    - path: 'src/components/DetailView/DetailView.test.tsx'
      provides: 'JSON raw-only visibility regression tests'
  key_links:
    - from: 'src/components/DetailView/scene/DetailScene.tsx'
      to: 'src/components/DetailView/scene/AlternateViews.tsx'
      via: 'raw-only alternate visibility and scrollable left column'
      pattern: 'showAlternateViews'
    - from: 'src/components/DetailView/ContentRenderers/JsonRenderer.tsx'
      to: 'src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx'
      via: 'explicit Monaco/tree height contract'
      pattern: 'contentHeight'
    - from: 'src/components/DetailView/DetailView.test.tsx'
      to: 'src/components/DetailView/scene/DetailScene.tsx'
      via: 'JSON raw-only detail regression'
      pattern: 'raw'
---

<objective>
闭合 UAT Test 3 的两个剩余回归：JSON 条目的 Raw 入口必须可达，长内容与代码视图必须可滚动且可见。

Purpose: 当前 detail scene 在 raw-only 场景里主动隐藏备用视图，又把 JSON 代码视图放进没有确定高度的容器中，导致用户同时丢失 Raw 切换入口和长内容滚动能力。这个计划只修复 JSON detail 的可达性与布局合同，不扩展到新的 preview 类型或 UI 大改版。
Output: raw-only 可见的 alternate views、可滚动的 detail 左列、显式高度的 JsonRenderer，以及对应的 detail/renderer 回归测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-UAT.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-04-SUMMARY.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-06-PLAN.md
@.planning/debug/json-raw-empty-no-scroll.md
@src/components/DetailView/scene/DetailScene.tsx
@src/components/DetailView/scene/AlternateViews.tsx
@src/components/DetailView/ContentRenderers/JsonRenderer.tsx
@src/components/DetailView/DetailView.test.tsx
@src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx

<interfaces>
当前 raw-only 和高度问题就在这些实现里：

```ts
const showAlternateViews = !(
  descriptor.alternateViews.length === 1 && descriptor.alternateViews[0]?.key === 'raw'
);
```

```ts
if (views.length === 1 && views[0]?.key === 'raw') {
  return null;
}
```

```tsx
<div className="min-h-0 flex-1 overflow-hidden" style={{ minHeight: contentHeight }}>
  <MonacoEditor height="100%" ... />
</div>
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 恢复 JSON raw-only 备用视图入口，并给 detail 左列建立稳定滚动容器</name>
  <files>src/components/DetailView/scene/DetailScene.tsx, src/components/DetailView/scene/AlternateViews.tsx, src/components/DetailView/scene/AlternateViews.test.tsx, src/components/DetailView/DetailView.test.tsx</files>
  <behavior>
    - Test 1: JSON 条目只有 `raw` 一个备用视图时，detail 仍显示 Raw 入口，而不是被 `showAlternateViews` / `AlternateViews` 双重隐藏。
    - Test 2: non-immersive detail 左列可以滚动访问长主预览和下方备用视图；immersive image/video/audio 仍可继续隐藏冗余 raw-only 视图。
    - Test 3: `02-06` 新增的 URL resolved media/text/json alternate 渲染能力不被回退掉。
  </behavior>
  <action>修改 `DetailScene.tsx`：把 raw-only 隐藏规则收紧到 immersive 媒体主视图，普通 JSON/code/markdown/plain_text 等非 immersive 布局必须显示 `AlternateViews`；同时把左列改成稳定的纵向滚动容器，让主预览和备用视图共用 `overflow-y-auto` 区域，而不是被多层 `overflow-hidden` 裁掉。修改 `AlternateViews.tsx`：删除“单一 raw 视图直接 return null”的逻辑，保留现有单卡片渲染路径。更新 `AlternateViews.test.tsx` 和 `DetailView.test.tsx`，把旧的“raw-only 隐藏备用视图”断言改成 JSON raw 可见回归，并保留 immersive media 场景继续由 `DetailPreviewContract.test.tsx` 约束。</action>
  <verify>
    <automated>pnpm test -- src/components/DetailView/scene/AlternateViews.test.tsx src/components/DetailView/DetailView.test.tsx</automated>
  </verify>
  <done>JSON 条目的 Raw 入口重新可达，detail 左列在长内容场景下可以滚动访问主预览与备用视图。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 给 JsonRenderer 的代码视图和树视图建立显式高度合同</name>
  <files>src/components/DetailView/ContentRenderers/JsonRenderer.tsx, src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx</files>
  <behavior>
    - Test 1: 切换到代码视图后，MonacoEditor 接收明确的 `height`，不再依赖未定义父高度的 `100%`。
    - Test 2: 树视图和无效 JSON 提示都位于同一显式高度容器中，长内容可以在内部滚动查看。
    - Test 3: 复制按钮仍然走 backend `copy_to_clipboard` 合同，不引入新的前端直写剪贴板路径。
  </behavior>
  <action>重写 `JsonRenderer.tsx` 的布局：保持 `contentHeight = 'clamp(360px, 52vh, 920px)'` 这类显式高度合同，但不要再只给父容器 `minHeight`；把 tree/code/invalid 三种分支都放到一个具备确定 `height` 和内部滚动的容器里，并让 Monaco 直接使用同一显式高度。更新 `JsonRenderer.test.tsx`，把现有 `data-height='100%'` 断言改成显式高度值，并补一条长内容/invalid-json 仍可见的回归断言。不要顺手重构 copy helper、theme 或其他 renderer。</action>
  <verify>
    <automated>pnpm test -- src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx</automated>
  </verify>
  <done>JsonRenderer 的树形/代码/无效 JSON 三个分支都拥有稳定高度和滚动容器，代码视图不会再空白。</done>
</task>

</tasks>

<verification>
先用 AlternateViews + DetailView 测试闭合 raw-only 可达性和 detail 滚动，再用 JsonRenderer 测试确认 Monaco 高度合同已稳定。
</verification>

<success_criteria>
用户复制合法 JSON 后，detail 仍会默认进入 JSON 结构化预览，但 Raw 入口可达；切换到代码视图时 Monaco 不再空白，超长内容与下方备用视图都可以滚动查看。
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-contracts-versioned-detection/02-07-SUMMARY.md`
</output>
