---
phase: 03-unified-developer-previews
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-03
files_modified:
  - src/components/DetailView/DetailView.tsx
  - src/components/DetailView/DetailView.test.tsx
  - src/components/DetailView/scene/PrimaryPreviewRenderer.tsx
  - src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx
  - src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx
autonomous: true
requirements:
  - PREV-04
  - PREV-05
must_haves:
  truths:
    - '尽管 `REQUIREMENTS.md` 的 PREV-04 原文写了 read-only，Phase 03 必须按 `03-CONTEXT.md` 的 D-14..D-17 实现：代码和命令 detail 默认是本地临时可编辑 workbench，不回写历史原文。'
    - '切换条目或关闭 detail 时，code / command workbench 的临时编辑缓冲会自动重置，即使两条记录的 raw content 恰好相同。'
    - 'detail 默认 copy 动作在 code / command 场景下复制当前 workbench buffer，而不是强制复制原始历史内容，落实 D-17。'
  artifacts:
    - path: src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx
      provides: '支持 session reset 与 buffer 上报的本地 workbench renderer'
    - path: src/components/DetailView/DetailView.tsx
      provides: 'detail-local workbench buffer 所有权与 copy delegation'
    - path: src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx
      provides: 'sessionKey reset / onContentChange contract tests'
    - path: src/components/DetailView/DetailView.test.tsx
      provides: 'detail copy uses active workbench buffer 的回归测试'
  key_links:
    - from: src/components/DetailView/scene/PrimaryPreviewRenderer.tsx
      to: src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx
      via: sessionKey and onContentChange props
      pattern: onContentChange
    - from: src/components/DetailView/DetailView.tsx
      to: src/components/DetailView/scene/DetailScene.tsx
      via: onCopy handler delegates to workbenchBuffer for code/command
      pattern: handleCopy
---

<objective>
闭合 code / command detail 的本地 workbench 合同，让可编辑、重置和复制当前编辑内容都落到真实 detail 交互路径里。

Purpose: 这是补充约束 2 和 D-14..D-17 的直接实现，也是对 PREV-04 wording 冲突的显式裁决。计划必须写清楚：以 CONTEXT 锁定决策为准，不做只读 renderer。
Output: 支持 session reset + buffer reporting 的 `UnifiedTextRenderer`，以及使用当前 workbench buffer 的 detail copy 行为。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-developer-previews/03-CONTEXT.md
@.planning/phases/03-unified-developer-previews/03-RESEARCH.md
@src/components/DetailView/DetailView.tsx
@src/components/DetailView/scene/PrimaryPreviewRenderer.tsx
@src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx
@src/components/DetailView/DetailView.test.tsx

<interfaces>
当前 renderer 入口：

```ts
interface UnifiedTextRendererProps {
  content: string;
  contentSubType: ContentSubType;
  metadata?: string | null;
}
```

本计划需要新增的 workbench props：

```ts
interface UnifiedTextRendererProps {
  content: string;
  contentSubType: ContentSubType;
  metadata?: string | null;
  sessionKey?: string;
  onContentChange?: (value: string) => void;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 workbench reset 与 copy-current-buffer 行为建立测试</name>
  <files>src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx, src/components/DetailView/DetailView.test.tsx</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, .planning/REQUIREMENTS.md, src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx, src/components/DetailView/DetailView.tsx, src/components/DetailView/DetailView.test.tsx</read_first>
  <behavior>
    - Test 1: `UnifiedTextRenderer` 在 `sessionKey` 改变时重置到新的 `content`，即使前后内容字符串相同，也能清空上一条条目的临时编辑，落实 D-16。
    - Test 2: `UnifiedTextRenderer` 每次编辑都会通过 `onContentChange` 上报当前 buffer，但不会写回 `selectedEntry.content_data`，落实 D-15。
    - Test 3: `DetailView` 在 code / command 场景下点击默认 copy 动作时，复制的是当前 workbench buffer；切换条目或关闭 detail 后缓冲重置，落实 D-17。
  </behavior>
  <action>新增 `src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx`，锁定 `sessionKey + onContentChange` 的 contract；扩展 `src/components/DetailView/DetailView.test.tsx`，新增 code / command 条目编辑后点击 header copy 按钮时调用 `copyToClipboard(editedValue)` 的用例，并补选中条目切换、selectedEntry=null 的重置断言。测试说明里要明确写出“PREV-04 read-only wording is overridden by D-14..D-17”，避免执行阶段验收口径再次分裂。</action>
  <acceptance_criteria>
    - `rg -n "sessionKey|onContentChange" src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx` 有匹配结果。
    - `rg -n "copyToClipboard|editedValue|selectedEntry" src/components/DetailView/DetailView.test.tsx` 有匹配结果。
    - `rg -n "D-14|D-15|D-16|D-17|read-only" src/components/DetailView/DetailView.test.tsx src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx src/components/DetailView/DetailView.test.tsx</automated>
  </verify>
  <done>可编辑 workbench 的 reset 与 copy 语义已被测试锁定，PREV-04 wording 冲突不再含糊。</done>
</task>

<task type="auto">
  <name>Task 2: 在 detail 路径里落地本地 workbench buffer 所有权和 copy delegation</name>
  <files>src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx, src/components/DetailView/scene/PrimaryPreviewRenderer.tsx, src/components/DetailView/DetailView.tsx, src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx, src/components/DetailView/DetailView.test.tsx</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, .planning/REQUIREMENTS.md, src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx, src/components/DetailView/scene/PrimaryPreviewRenderer.tsx, src/components/DetailView/DetailView.tsx, src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx, src/components/DetailView/DetailView.test.tsx</read_first>
  <action>修改 `UnifiedTextRenderer.tsx`，新增 `sessionKey` 与 `onContentChange` props；组件初始化和 `sessionKey` 改变时都重置本地 `editedContent`，并在编辑过程中持续上报当前 buffer。修改 `PrimaryPreviewRenderer.tsx`，只在 `subType === 'code'` 或 `subType === 'command'` 时把 `sessionKey=\`\${entry.id}:\${entry.content_hash}\`` 和 `onContentChange` 传给 `UnifiedTextRenderer`，不要把这套 workbench 状态扩散到 markdown / plain_text。修改 `DetailView.tsx`，新增 detail-local `workbenchBuffer` 状态：selectedEntry 改变或为 null 时清空；`handleCopy()` 在 code / command 场景下优先复制 `workbenchBuffer`，否则维持原有 raw copy 行为。不得把编辑结果写回 store 的 `entries` 或 `selectedEntry.content_data`，必须保持 D-15 的本地临时性。</action>
  <acceptance_criteria>
    - `rg -n "sessionKey|onContentChange" src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx src/components/DetailView/scene/PrimaryPreviewRenderer.tsx` 有匹配结果。
    - `rg -n "workbenchBuffer" src/components/DetailView/DetailView.tsx` 有匹配结果。
    - `rg -n "copyToClipboard\\((workbenchBuffer|activeWorkbenchBuffer)" src/components/DetailView/DetailView.tsx` 有匹配结果。
    - `rg -n "selectedEntry\\.content_data\\s*=" src/components/DetailView/DetailView.tsx src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx` 无匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx src/components/DetailView/DetailView.test.tsx</automated>
  </verify>
  <done>code / command detail 现在默认是本地临时 workbench；复制输出的是当前编辑 buffer；切换条目或关闭 detail 会自动重置临时编辑状态。</done>
</task>

</tasks>

<verification>
先运行 workbench renderer tests，再运行 DetailView tests，确认 editable buffer、copy delegation 和 session reset 都已闭合。
</verification>

<success_criteria>
用户在 code / command detail 里编辑的内容只在当前条目会话内生效，不回写历史；默认复制动作输出当前编辑缓冲；关闭或切换条目后缓冲自动清空。
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-developer-previews/03-05-SUMMARY.md`
</output>
