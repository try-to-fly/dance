---
phase: 03-unified-developer-previews
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/clipboard.ts
  - src/lib/preview/entryPresentation.ts
  - src/lib/preview/entryPresentation.test.ts
  - src/lib/preview/previewSummary.ts
  - src/lib/preview/previewSummary.test.ts
autonomous: true
requirements:
  - PREV-05
must_haves:
  truths:
    - '任意已识别条目都能仅基于 `entry.analysis`、本地 metadata 和 raw content 同步生成同一份 `SemanticPreviewModel`，不依赖 `resolveEntryPreview()` 或远端 URL enrichment。'
    - '列表与后续 retrieval 结果都通过 `buildPreviewSummary(..., density)` 共享相同的 `semanticType` 与 `previewIntent`，只允许信息密度不同。'
    - 'JSON、URL、颜色、代码、命令这些高频类型，以及 `plain_text`、`markdown`、`email`、`ip_address`、`timestamp`、`base64`、顶层 `image/file` 等 remaining types，都在 semantic core 里拥有显式 `semanticType` / `previewIntent` / headline / secondarySummary fallback 合同，不再让弱语义类型悄悄掉回原始文本截断。'
  artifacts:
    - path: src/types/clipboard.ts
      provides: 'Phase 03 的 `SemanticPreviewModel`、`PreviewIntent`、`PreviewSummaryDensity` 与 `PreviewSummaryDescriptor` 合同'
    - path: src/lib/preview/entryPresentation.ts
      provides: 'analysis-first `buildSemanticPreviewModel()` 入口，以及对旧 `buildEntrySemanticSummary()` 的兼容包装'
    - path: src/lib/preview/entryPresentation.test.ts
      provides: '高频类型 + remaining types 的 semantic core contract tests'
    - path: src/lib/preview/previewSummary.ts
      provides: '列表 / retrieval 共用的 density adapter'
    - path: src/lib/preview/previewSummary.test.ts
      provides: '同语义不同密度、且覆盖所有已识别类型 fallback 的 contract tests'
  key_links:
    - from: src/lib/preview/entryPresentation.ts
      to: src/lib/preview/previewSummary.ts
      via: buildPreviewSummary consumes buildSemanticPreviewModel
      pattern: buildSemanticPreviewModel
    - from: src/types/clipboard.ts
      to: src/lib/preview/entryPresentation.ts
      via: exported semantic contracts
      pattern: SemanticPreviewModel
---

<objective>
先建立 Phase 03 的共享 semantic preview core 和 density-aware summary contract，并完成当前所有已识别类型的 semantic sweep，让 list / detail / retrieval 三个表面都能从同一份语义核心派生，而不是继续各自解释 subtype。

Purpose: 这是 PREV-05 的基础。没有这层 contract，后续 list 改造、URL 本地结构卡、颜色 detail 和 code/command workbench 都会继续分叉。
Output: `SemanticPreviewModel`、`buildSemanticPreviewModel()`、`buildPreviewSummary()` 及其测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-unified-developer-previews/03-CONTEXT.md
@.planning/phases/03-unified-developer-previews/03-RESEARCH.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-06-SUMMARY.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-07-SUMMARY.md
@src/types/clipboard.ts
@src/lib/preview/entryPresentation.ts

<interfaces>
当前已存在的可复用接口：

```ts
export interface ClipboardEntry {
  content_type: string;
  content_data: string | null;
  content_subtype?: ContentSubType | string | null;
  metadata?: string | null;
  analysis?: EntryAnalysisSnapshot | null;
}

export interface EntrySemanticSummary {
  headline: string;
  normalizedText: string;
}
```

本计划需要新增并锁定的 contract：

```ts
export type PreviewSummaryDensity = 'list' | 'retrieval';

export interface SemanticPreviewModel {
  semanticType: string;
  previewIntent: string;
  headline: string;
  secondarySummary: string;
  rawContent: string | null;
  supportsRawView: boolean;
  usesWorkbench: boolean;
}

export interface PreviewSummaryDescriptor {
  density: PreviewSummaryDensity;
  semanticType: string;
  previewIntent: string;
  headline: string;
  secondarySummary: string;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 analysis-first `SemanticPreviewModel` 合同并完成全类型语义 sweep</name>
  <files>src/types/clipboard.ts, src/lib/preview/entryPresentation.ts, src/lib/preview/entryPresentation.test.ts</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, .planning/phases/03-unified-developer-previews/03-RESEARCH.md, src/types/clipboard.ts, src/lib/preview/entryPresentation.ts, src/lib/preview/entryPresentation.test.ts</read_first>
  <behavior>
    - Test 1: `buildSemanticPreviewModel()` 对 JSON、URL、颜色、代码、命令返回稳定的 `semanticType` 与 `previewIntent`，并继续以 `entry.analysis` 为唯一语义权威，落实 D-01、D-04。
    - Test 2: URL semantic model 仅依赖本地 `url_parts` / raw URL 生成 headline 与 secondary summary，不读取 `resolvedData`，落实 D-13 和“默认不依赖远端 URL enrichment”。
    - Test 3: 代码和命令 semantic model 显式标记 `usesWorkbench=true`、`supportsRawView=true`，为 D-14..D-17 的后续实现预留合同。
    - Test 4: `plain_text`、`markdown`、`email`、`ip_address`、`timestamp`、`base64`，以及顶层 `image` / `file` 都有显式 `semanticType` / `previewIntent` / headline / secondarySummary fallback 断言，不允许靠一个隐式 default 分支吞掉 remaining types，落实 D-03、D-04。
  </behavior>
  <action>在 `src/types/clipboard.ts` 新增 `PreviewIntent`、`SemanticPreviewModel` 等 Phase 03 contract；在 `src/lib/preview/entryPresentation.ts` 新增 `buildSemanticPreviewModel(entry)`，让它统一产出 `semanticType`、`previewIntent`、`headline`、`secondarySummary`、`rawContent`、`supportsRawView`、`usesWorkbench`。高频类型的 intent 名称固定为 `json_structured`、`url_structured`、`color_structured`、`code_workbench`、`command_workbench`，不要另起别名。除此之外，还要显式补齐 `plain_text`、`markdown`、`email`、`ip_address`、`timestamp`、`base64` 以及顶层 `image` / `file` 的 mapping table 与 fallback builder；`image` / `file` 继续按 `content_type` 优先，headline 优先文件名，secondary summary 回退到规范化 raw preview 或 metadata hint；`timestamp` 用 metadata 优先格式化、缺失 metadata 时保留原值；`email` / `ip_address` / `markdown` / `plain_text` / `base64` 都必须有可预测的 secondary summary 规则。`buildEntrySemanticSummary()` 保留，但必须改为从 `buildSemanticPreviewModel()` 派生，避免形成第二套 headline 逻辑。测试直接扩展 `src/lib/preview/entryPresentation.test.ts`，不要新建第三套临时 helper。这个任务只建立 semantic core，不要提前修改 `ClipboardItem.tsx`、`previewDescriptor.ts` 或搜索功能本身。</action>
  <acceptance_criteria>
    - `rg -n "export (type|interface) (PreviewIntent|SemanticPreviewModel)" src/types/clipboard.ts` 有匹配结果。
    - `rg -n "export const buildSemanticPreviewModel" src/lib/preview/entryPresentation.ts` 有匹配结果。
    - `rg -n "json_structured|url_structured|code_workbench|command_workbench|usesWorkbench" src/lib/preview/entryPresentation.ts src/lib/preview/entryPresentation.test.ts` 有匹配结果。
    - `rg -n "plain_text|markdown|email|ip_address|timestamp|base64" src/lib/preview/entryPresentation.ts src/lib/preview/entryPresentation.test.ts` 有匹配结果。
    - `rg -n "image|file" src/lib/preview/entryPresentation.ts src/lib/preview/entryPresentation.test.ts` 有匹配结果，证明顶层 non-text 类型也被纳入 semantic contract。
    - `rg -n "resolveEntryPreview|resolvedData" src/lib/preview/entryPresentation.ts` 无匹配结果，证明 semantic core 不依赖异步 preview resolution。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/preview/entryPresentation.test.ts</automated>
  </verify>
  <done>`buildSemanticPreviewModel()` 已成为 list/detail/retrieval 共享的单一语义入口，高频 developer 类型与 remaining types 的 semantic type / preview intent / fallback summary 全都被测试锁死。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 list / retrieval 共用的 density adapter，但不提前实现搜索</name>
  <files>src/types/clipboard.ts, src/lib/preview/previewSummary.ts, src/lib/preview/previewSummary.test.ts, src/lib/preview/entryPresentation.ts</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, .planning/phases/03-unified-developer-previews/03-RESEARCH.md, src/types/clipboard.ts, src/lib/preview/entryPresentation.ts</read_first>
  <behavior>
    - Test 1: `buildPreviewSummary(entry, 'list')` 与 `buildPreviewSummary(entry, 'retrieval')` 共享同一 `semanticType` / `previewIntent`，落实 D-04、D-09、PREV-05。
    - Test 2: `list` 密度保持紧凑，`retrieval` 密度只增加 secondary summary 信息量，不新增 subtype 推断，也不开始实现搜索匹配本身。
    - Test 3: URL / JSON / 颜色 / 代码 / 命令五类在 `previewSummary.test.ts` 都有明确 headline + secondary summary 断言，防止后续列表回退成原始文本截断。
    - Test 4: `plain_text`、`markdown`、`email`、`ip_address`、`timestamp`、`base64`、顶层 `image` / `file` 也在 `previewSummary.test.ts` 里有显式 list / retrieval 合同断言，不留 orphan subtype 分支。
  </behavior>
  <action>新增 `src/lib/preview/previewSummary.ts`，导出 `PreviewSummaryDensity = 'list' | 'retrieval'`、`PreviewSummaryDescriptor`、`buildPreviewSummary(entry, density)`。`list` 模式必须面向 D-05..D-08 的紧凑两层摘要；`retrieval` 模式只用于为 Phase 4 预留更高信息密度的 contract，不要在这个任务里实现搜索 UI、查询匹配、高亮或过滤。把所有 summary 都建立在 `buildSemanticPreviewModel()` 之上，禁止在 `previewSummary.ts` 里重新 parse `entry.metadata` 或 `entry.content_subtype`。新增 `src/lib/preview/previewSummary.test.ts`，除了五类高频类型外，还要按 RESEARCH 里的 coverage strategy 把 lightweight semantic cards（`timestamp`、`email`、`ip_address`、`markdown`、`plain_text`）、`base64`、顶层 `image/file` 的 headline + secondary summary fallback 一并锁进 `list` / `retrieval` contract，避免某些 subtype 只在 detail 有语义、到了 list/retrieval 又退回原始文本截断。</action>
  <acceptance_criteria>
    - `rg -n "export (type|interface) PreviewSummary(Density|Descriptor)|buildPreviewSummary" src/lib/preview/previewSummary.ts src/types/clipboard.ts` 有匹配结果。
    - `rg -n "retrieval" src/lib/preview/previewSummary.ts src/lib/preview/previewSummary.test.ts` 有匹配结果，证明已为 Phase 4 预留 retrieval contract。
    - `rg -n "plain_text|markdown|email|ip_address|timestamp|base64|image|file" src/lib/preview/previewSummary.test.ts` 有匹配结果。
    - `rg -n "searchTerm|highlight|fuzzy" src/lib/preview/previewSummary.ts` 无匹配结果，证明没有提前实现 Phase 4 搜索功能。
    - `rg -n "buildSemanticPreviewModel" src/lib/preview/previewSummary.ts` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/preview/entryPresentation.test.ts src/lib/preview/previewSummary.test.ts</automated>
  </verify>
  <done>共享 summary contract 已可为 list 和未来 retrieval 复用，且所有当前已识别类型都具备显式 density-aware summary fallback；检索能力本身仍保持 deferred。</done>
</task>

</tasks>

<verification>
先跑 `entryPresentation` tests 锁定 semantic core 和 remaining-types sweep，再跑 `previewSummary` tests 锁定 list / retrieval 密度合同，确认两个模块都不依赖异步 preview resolution 或远端 URL enrichment，且不会让弱语义类型回退成隐式原始文本截断。
</verification>

<success_criteria>
Phase 03 后续所有 list/detail/retrieval 计划都可以直接消费 `SemanticPreviewModel` 与 `buildPreviewSummary()`，不再继续新增平行语义入口。
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-developer-previews/03-01-SUMMARY.md`
</output>
