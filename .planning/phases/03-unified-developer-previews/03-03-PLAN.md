---
phase: 03-unified-developer-previews
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/stores/clipboardStore.ts
  - src/stores/clipboardStore.test.ts
  - src/lib/preview/previewDescriptor.ts
  - src/components/DetailView/DetailPreviewContract.test.tsx
  - src/components/DetailView/scene/PrimaryPreviewRenderer.tsx
  - src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx
  - src/components/DetailView/scene/AlternateViews.tsx
  - src/components/DetailView/ContentRenderers/UrlCardRenderer.tsx
autonomous: true
requirements:
  - PREV-01
  - PREV-02
  - PREV-05
must_haves:
  truths:
    - 'URL 条目的默认 detail 主路径完全依赖本地 `url_parts` 和 raw URL，即使没有任何远端 resolved data 也能展示 protocol、host、path、query，落实 D-13 与 PREV-02。'
    - '默认 detail 打开 URL 时不会再自动依赖 `resolveUrlPreview()` 的远端 enrichment；Raw 入口与本地 URL 结构卡都稳定可达。'
    - 'JSON 条目的结构化主视图与 Raw 稳定入口在 descriptor / renderer 重构后仍保持可用，PREV-01 不会因 URL 本地化而回退。'
  artifacts:
    - path: src/components/DetailView/ContentRenderers/UrlCardRenderer.tsx
      provides: '纯本地 URL 结构卡 renderer'
    - path: src/stores/clipboardStore.ts
      provides: 'URL detail 默认不触发远端 preview resolution 的 store contract'
    - path: src/lib/preview/previewDescriptor.ts
      provides: '消费 semantic core 的 detail descriptor，并保留 Raw / URL structure alternates'
    - path: src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx
      provides: 'URL local card 与 JSON primary renderer 的回归测试'
  key_links:
    - from: src/stores/clipboardStore.ts
      to: src/lib/preview/previewDescriptor.ts
      via: resolveEntryPreview no longer drives default URL semantics
      pattern: resolveEntryPreview
    - from: src/components/DetailView/scene/PrimaryPreviewRenderer.tsx
      to: src/components/DetailView/ContentRenderers/UrlCardRenderer.tsx
      via: kind === 'url_card'
      pattern: UrlCardRenderer
    - from: src/lib/preview/previewDescriptor.ts
      to: src/components/DetailView/DetailPreviewContract.test.tsx
      via: Raw + semantic alternate contract
      pattern: buildPreviewDescriptor
---

<objective>
把 URL detail 主体验彻底收口为纯本地结构卡，并让 detail descriptor 明确建立在 semantic core 上，而不是再让默认路径依赖远端 URL enrichment。

Purpose: 这是对 D-10..D-13 和补充约束 2/3/4 的直接承接。Phase 2 已锁定 URL-first，但当前 detail 默认仍会自动跑远端 resolution；Phase 3 必须把“默认可读、默认本地”落到真实生产路径。
Output: 本地 `UrlCardRenderer`、停止默认 URL 远端解析的 store contract、更新后的 descriptor / renderer tests。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-unified-developer-previews/03-CONTEXT.md
@.planning/phases/03-unified-developer-previews/03-RESEARCH.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-06-SUMMARY.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-07-SUMMARY.md
@src/stores/clipboardStore.ts
@src/lib/preview/previewDescriptor.ts
@src/components/DetailView/scene/PrimaryPreviewRenderer.tsx
@src/components/DetailView/scene/AlternateViews.tsx

<interfaces>
当前 detail 关键接口：

```ts
export interface PreviewDescriptor {
  headline: string;
  typeLabel: string;
  badges: PreviewBadge[];
  primaryKind: PreviewKind;
  primaryPayload: unknown;
  inspectorSections: PreviewInspectorSection[];
  alternateViews: PreviewAlternateView[];
  actions: PreviewAction[];
}
```

本计划需要新增的 URL renderer 形状：

```ts
interface UrlCardRendererProps {
  raw: string;
  parts?: {
    protocol: string;
    host: string;
    path: string;
    query_params: Array<[string, string]>;
  } | null;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为本地 URL 结构卡与默认无远端依赖的 detail contract 建立测试</name>
  <files>src/stores/clipboardStore.test.ts, src/components/DetailView/DetailPreviewContract.test.tsx, src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, .planning/phases/03-unified-developer-previews/03-RESEARCH.md, src/stores/clipboardStore.ts, src/stores/clipboardStore.test.ts, src/lib/preview/previewDescriptor.ts, src/components/DetailView/DetailPreviewContract.test.tsx, src/components/DetailView/scene/PrimaryPreviewRenderer.tsx</read_first>
  <behavior>
    - Test 1: `resolveEntryPreview()` 对 URL 条目默认不再调用 `resolveUrlPreview()`，落实“默认不依赖远端 URL enrichment”。
    - Test 2: `PrimaryPreviewRenderer` 在 `kind='url_card'` 时展示 protocol、host、path、query 明细，而不是原始 code block，也不引入旧 `UrlRenderer`。
    - Test 3: JSON 条目的 structured primary view + Raw 入口在更新 descriptor 之后仍保持成立，确保 PREV-01 没被 URL 改造破坏。
  </behavior>
  <action>扩展 `src/stores/clipboardStore.test.ts`，新增 URL 条目默认不触发 `resolveUrlPreview()` 的回归；新增 `src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx`，直接断言本地 URL 结构卡至少显示 Protocol / Host / Path / Query；同步调整 `DetailPreviewContract.test.tsx`，让 URL 条目断言 `raw + url-structure` 始终存在，JSON 条目继续断言 `primaryKind='json'` 且保留 `raw` alternate。测试里不要 mock 或导入旧 `UrlRenderer.tsx`，以免把废弃路径重新锁回主合同。</action>
  <acceptance_criteria>
    - `rg -n "resolveUrlPreview" src/stores/clipboardStore.test.ts` 有匹配结果，且新增测试断言 URL 默认不调用它。
    - `rg -n "Protocol|Host|Path|Query" src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx` 有匹配结果。
    - `rg -n "raw|url-structure|primaryKind.*json" src/components/DetailView/DetailPreviewContract.test.tsx` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/stores/clipboardStore.test.ts src/components/DetailView/DetailPreviewContract.test.tsx src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx</automated>
  </verify>
  <done>默认 URL detail 本地化与 JSON Raw 稳定入口都已由测试锁定，后续实现不会把旧 URL renderer 或远端依赖重新带回主路径。</done>
</task>

<task type="auto">
  <name>Task 2: 落地纯本地 URL card renderer，并关闭默认 URL 远端解析依赖</name>
  <files>src/stores/clipboardStore.ts, src/lib/preview/previewDescriptor.ts, src/components/DetailView/scene/PrimaryPreviewRenderer.tsx, src/components/DetailView/scene/AlternateViews.tsx, src/components/DetailView/ContentRenderers/UrlCardRenderer.tsx, src/stores/clipboardStore.test.ts, src/components/DetailView/DetailPreviewContract.test.tsx, src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, .planning/phases/03-unified-developer-previews/03-RESEARCH.md, src/stores/clipboardStore.ts, src/lib/preview/previewDescriptor.ts, src/components/DetailView/scene/PrimaryPreviewRenderer.tsx, src/components/DetailView/scene/AlternateViews.tsx, src/components/DetailView/DetailPreviewContract.test.tsx, src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx</read_first>
  <action>在 `src/components/DetailView/ContentRenderers/UrlCardRenderer.tsx` 新建纯本地 URL renderer，明确展示 protocol、host、path、query 参数数量与 key/value 列表，必要时同时显示 raw URL。更新 `PrimaryPreviewRenderer.tsx` 和 `AlternateViews.tsx`，让 `url_card` 统一走这个本地 renderer，不再把 URL card 简化成 code block。更新 `previewDescriptor.ts`：消费 03-01 的 `buildSemanticPreviewModel()` 作为 headline/type 的单一来源，并继续保留 `Raw` 与 `url-structure` alternate；不要把旧 `UrlRenderer` 拉回主路径。更新 `src/stores/clipboardStore.ts`：`resolveEntryPreview()` 在 `subType === 'url'` 时默认直接返回本地可用数据，不再自动调用 `resolveUrlPreview(entry.content_data)`；`resolveUrlPreview()` helper 可保留给后续显式 enrichment，但这个计划不要接任何新的按钮或搜索能力。</action>
  <acceptance_criteria>
    - `rg -n "UrlCardRenderer" src/components/DetailView/scene/PrimaryPreviewRenderer.tsx src/components/DetailView/scene/AlternateViews.tsx src/components/DetailView/ContentRenderers/UrlCardRenderer.tsx` 有匹配结果。
    - `rg -n "resolveUrlPreview\\(entry\\.content_data\\)" src/stores/clipboardStore.ts` 无匹配结果。
    - `rg -n "buildSemanticPreviewModel" src/lib/preview/previewDescriptor.ts` 有匹配结果。
    - `rg -n "UrlRenderer" src/components/DetailView/scene/PrimaryPreviewRenderer.tsx src/components/DetailView/scene/AlternateViews.tsx src/lib/preview/previewDescriptor.ts` 无匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/stores/clipboardStore.test.ts src/components/DetailView/DetailPreviewContract.test.tsx src/components/DetailView/scene/PrimaryPreviewRenderer.test.tsx</automated>
  </verify>
  <done>URL detail 默认主路径已经是纯本地结构卡，Raw 可达，JSON structured/raw contract 仍稳定，且默认不再依赖远端 URL enrichment。</done>
</task>

</tasks>

<verification>
先验证 store contract，确保 URL 默认不再自动远端解析；再验证 PrimaryPreviewRenderer 和 descriptor tests，确保 URL card / JSON Raw 都维持稳定。
</verification>

<success_criteria>
用户即使离线也能在 detail 里稳定看到 URL 的协议、host、path、query；JSON 结构化主视图与 Raw 切换保持不变；默认路径不再依赖远端 URL enrichment。
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-developer-previews/03-03-SUMMARY.md`
</output>
