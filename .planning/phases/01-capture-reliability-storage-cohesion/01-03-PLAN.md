---
phase: 01-capture-reliability-storage-cohesion
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/App.tsx
  - src/stores/clipboardStore.ts
  - src/stores/clipboardStore.test.ts
  - src/components/MenuEventHandler/MenuEventHandler.tsx
autonomous: true
requirements:
  - CAPT-02
must_haves:
  truths:
    - '应用内的复制动作会先经过 Rust 命令边界，给后端自写抑制留出注册窗口。'
    - '前端事件订阅在重复挂载、HMR 或重新初始化时不会叠加成多个监听器。'
    - '用户从历史重新复制内容时，不会因为前端绕过后端而再次写出重复历史。'
  artifacts:
    - path: 'src/stores/clipboardStore.ts'
      provides: '后端 copy 命令接线与 idempotent clipboard-update listener'
    - path: 'src/stores/clipboardStore.test.ts'
      provides: '前端监听器与 copy 路由回归测试'
    - path: 'src/components/MenuEventHandler/MenuEventHandler.tsx'
      provides: 'React effect 级事件清理'
  key_links:
    - from: 'src/stores/clipboardStore.ts'
      to: 'src-tauri/src/commands.rs'
      via: "invoke('copy_to_clipboard')"
      pattern: 'copy_to_clipboard'
    - from: 'src/App.tsx'
      to: 'src/stores/clipboardStore.ts'
      via: 'setupEventListener() cleanup'
      pattern: 'setupEventListener'
    - from: 'src/components/MenuEventHandler/MenuEventHandler.tsx'
      to: '@tauri-apps/api/event'
      via: 'unlisten cleanup'
      pattern: "return \\(\\) =>"
---

<objective>
补齐 CAPT-02 在前端侧的最后一段链路：所有应用内复制都必须经过 Rust，所有事件监听都必须有明确清理边界。

Purpose: 后端运行时和抑制逻辑已经到位后，前端如果继续直接 `writeText()` 或重复注册监听，仍然会制造“重复保存”错觉。
Output: 后端 copy 命令接线、可清理的 `clipboard-update` 监听器、React effect cleanup，以及对应 Vitest 回归。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md
@src/App.tsx
@src/stores/clipboardStore.ts
@src/stores/clipboardStore.test.ts
@src/components/MenuEventHandler/MenuEventHandler.tsx
@src-tauri/src/commands.rs

<interfaces>
From `src/stores/clipboardStore.ts`:

```ts
startMonitoring: () => Promise<void>;
stopMonitoring: () => Promise<void>;
copyToClipboard: (content: string) => Promise<void>;
setupEventListener: () => void;
```

From `src-tauri/src/commands.rs`:

```rust
#[tauri::command]
pub async fn copy_to_clipboard(
    app: tauri::AppHandle,
    state: State<'_, AppState>,
    content: String,
) -> Result<(), String>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把应用内复制统一切到 Rust copy 命令</name>
  <files>src/stores/clipboardStore.ts, src/stores/clipboardStore.test.ts</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src/stores/clipboardStore.ts, src/stores/clipboardStore.test.ts, src-tauri/src/commands.rs</read_first>
  <behavior>
    - Test 1: `copyToClipboard('hello')` 调用 `invoke('copy_to_clipboard', { content: 'hello' })`，不再调用 `@tauri-apps/plugin-clipboard-manager.writeText`。
    - Test 2: copy 调用失败时继续把错误写入 store 的 `error` 字段，而不是静默吞掉。
  </behavior>
  <action>在 `src/stores/clipboardStore.ts` 里删除 `writeText` 直写路径，把 `copyToClipboard` 的实现改成 `await invoke('copy_to_clipboard', { content })`。`pasteSelectedEntry` 保持现有 `paste_text` / `paste_image` 命令。`src/stores/clipboardStore.test.ts` 里新增针对 `copyToClipboard` 的断言，明确验证 `invokeMock` 接收到命令名 `copy_to_clipboard` 和参数对象 `{ content: '...' }`，同时验证原来的 `writeText` mock 不再被调用。不要把 CAPT-02 继续寄托在前端插件调用上。</action>
  <acceptance_criteria>
    - `rg -n "invoke\\('copy_to_clipboard', \\{ content \\}\\)" src/stores/clipboardStore.ts` 有匹配结果。
    - `rg -n "writeText" src/stores/clipboardStore.ts` 无匹配结果。
    - `rg -n "copy_to_clipboard" src/stores/clipboardStore.test.ts` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/stores/clipboardStore.test.ts</automated>
  </verify>
  <done>应用内文本复制已统一经过 Rust 命令边界，后端自写抑制不再被前端绕过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 clipboard-update 与菜单事件监听具备显式 cleanup 边界</name>
  <files>src/App.tsx, src/stores/clipboardStore.ts, src/stores/clipboardStore.test.ts, src/components/MenuEventHandler/MenuEventHandler.tsx</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src/App.tsx, src/stores/clipboardStore.ts, src/stores/clipboardStore.test.ts, src/components/MenuEventHandler/MenuEventHandler.tsx</read_first>
  <behavior>
    - Test 1: 连续两次调用 `setupEventListener()` 时，会先执行上一次 `unlisten`，最终只保留一个 `clipboard-update` 订阅。
    - Test 2: `App.tsx` 与 `MenuEventHandler` 在 effect cleanup 时都会调用真实 `unlisten`，不会把异步返回值丢掉。
  </behavior>
  <action>把 `setupEventListener` 改成返回 cleanup 的异步函数，例如 `() => Promise<() => void>`。在 store 内部持有唯一的 `clipboardUpdateUnlisten` 引用；每次重新 setup 前都先执行旧 cleanup，再注册新的 `listen('clipboard-update', ...)`。`src/App.tsx` 的启动 effect 必须 `await setupEventListener()` 并在 `return () => { cleanup?.(); }` 中回收监听器，不能再 fire-and-forget。`src/components/MenuEventHandler/MenuEventHandler.tsx` 也必须把 `listen(...)` 返回的四个 `unlisten` 汇总到真正的 effect cleanup，而不是让 `setupListeners()` 的返回值丢失。补 Vitest：mock `listen` 返回不同的 unlisten 函数，验证第二次 setup 会调用第一次 cleanup，并且 effect cleanup 时所有 unlisten 都被调用一次。</action>
  <acceptance_criteria>
    - `rg -n "clipboardUpdateUnlisten|setupEventListener: async|return \\(\\) =>" src/stores/clipboardStore.ts` 有匹配结果。
    - `rg -n "setupEventListener\\(\\)" src/App.tsx` 有匹配结果，且 `src/App.tsx` 中存在 effect cleanup 的 `return () =>`.
    - `rg -n "unlistenMonitoring|unlistenHistory|unlistenPreferences|unlistenGlobalShortcut" src/components/MenuEventHandler/MenuEventHandler.tsx` 有匹配结果，并且文件中存在顶层 `return () =>`.
    - `rg -n "listen" src/stores/clipboardStore.test.ts` 有新增监听器 cleanup 断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/stores/clipboardStore.test.ts</automated>
  </verify>
  <done>前端事件监听具备可重复初始化与可靠清理语义，不会再因为重复挂载或 HMR 累积多个 `clipboard-update` 监听器。</done>
</task>

</tasks>

<verification>
运行 `pnpm test -- src/stores/clipboardStore.test.ts`，并确认新增断言同时覆盖 copy 命令接线与监听器 cleanup。若 store API 签名变化导致 `src/App.tsx` 受影响，再补跑 `pnpm test` 进行全局回归。
</verification>

<success_criteria>
前端不再绕过 Rust copy 命令，且所有长生命周期事件订阅都能在重复初始化和组件卸载时被正确清理。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-03-SUMMARY.md`
</output>
