---
phase: 01-capture-reliability-storage-cohesion
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/capture/mod.rs
  - src-tauri/src/capture/runtime.rs
  - src-tauri/src/database/mod.rs
  - src-tauri/src/clipboard/monitor.rs
  - src-tauri/src/state.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/capture_runtime_tests.rs
autonomous: true
requirements:
  - CAPT-01
  - CAPT-02
must_haves:
  truths:
    - '开始监听后只会存在一个受控运行时，而不是不断叠加的后台任务。'
    - '停止监听后轮询与持久化任务都会被显式取消并等待退出。'
    - '重复开始、停止和重启不会生成重复保存 worker，也不会把同一内容写成多条历史。'
  artifacts:
    - path: 'src-tauri/src/capture/runtime.rs'
      provides: 'CaptureRuntime 运行时所有权、取消与 join 语义'
      exports:
        - 'CaptureRuntime'
        - 'SuppressionEntry'
    - path: 'src-tauri/src/capture_runtime_tests.rs'
      provides: 'CAPT-01/CAPT-02 生命周期与去重测试'
  key_links:
    - from: 'src-tauri/src/state.rs'
      to: 'src-tauri/src/capture/runtime.rs'
      via: 'start_monitoring()/stop_monitoring()'
      pattern: 'CaptureRuntime'
    - from: 'src-tauri/src/capture/runtime.rs'
      to: 'src-tauri/src/database/mod.rs'
      via: 'UPSERT save loop'
      pattern: "ON CONFLICT\\(content_hash\\)"
    - from: 'src-tauri/src/capture/runtime.rs'
      to: 'src-tauri/src/clipboard/monitor.rs'
      via: 'runtime-owned monitor loop'
      pattern: 'CancellationToken'
---

<objective>
把剪贴板监听从 fire-and-forget 任务改成可停止、可等待退出、只存在一个实例的 `CaptureRuntime`。

Purpose: 这是 CAPT-01 的核心修复，同时为 CAPT-02 的单 worker 去重和自写抑制提供唯一所有权边界。
Output: `CaptureRuntime`、`CancellationToken` 生命周期控制、单 save worker、`UPSERT` 去重与对应自动化测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md
@src-tauri/src/app_paths.rs
@src-tauri/src/state.rs
@src-tauri/src/clipboard/monitor.rs
@src-tauri/src/database/mod.rs
@src-tauri/Cargo.toml

<interfaces>
From `src-tauri/src/state.rs`:

```rust
pub async fn start_monitoring(&self) -> Result<()>;
pub async fn stop_monitoring(&self) -> Result<()>;
pub async fn is_monitoring(&self) -> bool;
```

From `src-tauri/src/clipboard/monitor.rs`:

```rust
pub struct ClipboardMonitor { ... }
pub async fn start_monitoring(&self);
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 引入 CaptureRuntime 并把监听与保存任务收归一个所有者</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/capture/mod.rs, src-tauri/src/capture/runtime.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/state.rs, src-tauri/src/database/mod.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src-tauri/Cargo.toml, src-tauri/src/state.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/database/mod.rs</read_first>
  <behavior>
    - Test 1: `start_monitoring()` 之后创建 1 个运行时；再次调用 `start_monitoring()` 不会再创建第二个 monitor loop 或第二个 save loop。
    - Test 2: `stop_monitoring()` 会 cancel 并 await monitor/save 两个任务；停止后不再处理新条目。
    - Test 3: 同一 `content_hash` 的保存走单条 SQL `UPSERT`，结果是单行记录加 `copy_count` 递增，而不是多行重复记录。
  </behavior>
  <action>在 `src-tauri/Cargo.toml` 增加 `tokio-util = "0.7"`。新增 `src-tauri/src/capture/runtime.rs` 和 `src-tauri/src/capture/mod.rs`，实现 `CaptureRuntime`，字段至少包含：`cancel: CancellationToken`、`monitor_task: JoinHandle<()>`、`save_task: JoinHandle<()>`、`suppression: Arc<Mutex<Vec<SuppressionEntry>>>`。`SuppressionEntry` 固定使用 `content_hash: String` 和 `expires_at_ms: i64`。把 `ClipboardMonitor::start_monitoring()` 改成由运行时拥有的 async loop，loop 内使用 `tokio::select! { _ = cancel.cancelled() => break, _ = sleep(Duration::from_millis(500)) => ... }`。`AppState` 中原来的 `monitor: Arc<RwLock<Option<ClipboardMonitor>>>` 必须替换为 `capture_runtime: Arc<RwLock<Option<CaptureRuntime>>>`，`stop_monitoring()` 必须 `take()` 出 runtime、调用 `runtime.stop().await`，然后再返回成功。数据库保存逻辑不要再保留独立 `start_database_save_task()`；把它迁入 runtime save loop，并把去重 SQL 改成唯一索引加 `INSERT ... ON CONFLICT(content_hash) DO UPDATE SET copy_count = clipboard_entries.copy_count + 1, created_at = excluded.created_at, source_app = excluded.source_app, content_subtype = excluded.content_subtype, metadata = excluded.metadata, app_bundle_id = excluded.app_bundle_id, file_path = COALESCE(excluded.file_path, clipboard_entries.file_path)`。同时在 `Database::init()` 加 `CREATE UNIQUE INDEX IF NOT EXISTS idx_clipboard_entries_content_hash_unique ON clipboard_entries(content_hash)`。</action>
  <acceptance_criteria>
    - `rg -n "tokio-util|tokio_util" src-tauri/Cargo.toml src-tauri/src/capture/runtime.rs src-tauri/src/state.rs` 有匹配结果。
    - `rg -n "CaptureRuntime|SuppressionEntry|CancellationToken|JoinHandle" src-tauri/src/capture/runtime.rs src-tauri/src/state.rs` 有匹配结果。
    - `rg -n "idx_clipboard_entries_content_hash_unique|ON CONFLICT\\(content_hash\\)" src-tauri/src/database/mod.rs src-tauri/src/capture/runtime.rs src-tauri/src/state.rs` 有匹配结果。
    - `rg -n "start_database_save_task" src-tauri/src/state.rs` 无匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_capture_runtime_stop_cancels_tasks -- --nocapture</automated>
  </verify>
  <done>`CaptureRuntime` 成为唯一监听/保存所有者，CAPT-01 所需的显式停止和 join 语义已经落地。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补齐 CAPT-01/CAPT-02 自动化覆盖并固定运行时回归信号</name>
  <files>src-tauri/src/capture_runtime_tests.rs, src-tauri/src/lib.rs, src-tauri/src/test_support.rs, src-tauri/src/capture/runtime.rs, src-tauri/src/state.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md, src-tauri/src/test_support.rs, src-tauri/src/capture/runtime.rs, src-tauri/src/state.rs, src-tauri/src/lib.rs</read_first>
  <behavior>
    - Test 1: `test_capture_runtime_stop_cancels_tasks` 在 stop 后不会再消费任何模拟 clipboard 事件。
    - Test 2: `test_capture_runtime_single_worker_and_suppression` 在重复 start 与重复 hash 输入下只保留一行记录，并让 `copy_count` 精确递增。
    - Test 3: `test_capture_runtime_restart_is_single_owner` 证明 stop 后 restart 仍然只存在 1 个 runtime owner。
  </behavior>
  <action>新增 `src-tauri/src/capture_runtime_tests.rs`，并在 `src-tauri/src/lib.rs` 中按现有 `state_tests` 风格注册它。测试必须使用 Plan 01 的 `test_support` temp-root helper，禁止读写真实用户目录。实现三个明确测试名：`test_capture_runtime_stop_cancels_tasks`、`test_capture_runtime_single_worker_and_suppression`、`test_capture_runtime_restart_is_single_owner`。测试里要直接验证数据库结果：相同 hash 最终只有一条记录，`copy_count` 从 1 到 2 递增；停止后新的模拟事件不会再写入数据库；重复 start 不会创建第二个 worker。不要写 watch 模式，不要用 sleep 代替 join 成功作为唯一断言。</action>
  <acceptance_criteria>
    - `rg -n "test_capture_runtime_stop_cancels_tasks|test_capture_runtime_single_worker_and_suppression|test_capture_runtime_restart_is_single_owner" src-tauri/src/capture_runtime_tests.rs` 有 3 个测试名。
    - `rg -n "create_temp_app_paths|TempDir" src-tauri/src/capture_runtime_tests.rs src-tauri/src/test_support.rs` 有匹配结果。
    - `rg -n "copy_count" src-tauri/src/capture_runtime_tests.rs` 有匹配结果，表明测试直接检查去重结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_capture_runtime_single_worker_and_suppression -- --nocapture</automated>
  </verify>
  <done>CAPT-01/CAPT-02 的运行时回归都有可复现自动化信号，后续前端接线或策略改动可以快速发现监听生命周期回退。</done>
</task>

</tasks>

<verification>
依次运行 `cd src-tauri && cargo test test_capture_runtime_stop_cancels_tasks -- --nocapture`、`cd src-tauri && cargo test test_capture_runtime_single_worker_and_suppression -- --nocapture`，最后跑一次 `cd src-tauri && cargo test` 确认现有状态层测试仍然通过。
</verification>

<success_criteria>
开始/停止监听已经变成真正可控的运行时行为，重复 start/stop 不会堆叠后台任务，同一内容的持久化结果只保留一行并累计复制次数。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-02-SUMMARY.md`
</output>
