---
phase: 01-capture-reliability-storage-cohesion
plan: 06
type: execute
wave: 6
gap_closure: true
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src-tauri/src/clipboard/processor.rs
  - src-tauri/src/clipboard/monitor.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/app_paths_tests.rs
autonomous: true
requirements:
  - CAPT-04
must_haves:
  truths:
    - '图片 metadata 回读与日志命令都只通过 `AppPaths` 解析本地路径，不再保留 legacy 或手写根目录。'
    - '日志文件定位固定为 `AppPaths::log_dir()/clipboard-app.log`，缺失日志仍返回空字符串，清理缺失日志仍保持 no-op。'
    - 'CAPT-04 至少有一条自动化回归覆盖日志路径，并有代码级信号证明 monitor 的图片回读复用 `resolve_relative_asset_path()`。'
  artifacts:
    - path: 'src-tauri/src/clipboard/monitor.rs'
      provides: '图片 metadata 回读改走 AppPaths'
    - path: 'src-tauri/src/commands.rs'
      provides: '日志读取/清理改走 AppPaths::log_dir()'
    - path: 'src-tauri/src/app_paths_tests.rs'
      provides: 'CAPT-04 gap closure 回归测试'
  key_links:
    - from: 'src-tauri/src/app_paths.rs'
      to: 'src-tauri/src/clipboard/monitor.rs'
      via: 'resolve_relative_asset_path()'
      pattern: 'resolve_relative_asset_path'
    - from: 'src-tauri/src/app_paths.rs'
      to: 'src-tauri/src/commands.rs'
      via: 'log_dir()'
      pattern: 'log_dir'
    - from: 'src-tauri/src/commands.rs'
      to: 'src-tauri/src/app_paths_tests.rs'
      via: 'log helper regression'
      pattern: 'app_log_file_path'
---

<objective>
闭合 `CAPT-04` 的最后两个生产路径绕路，让图片 metadata 回读和日志读写都真正服从 `AppPaths`。

Purpose: verifier 已确认主存储链路基本完成，这个 gap closure 只修 residual bypass，不重做 Phase 1 其他能力。
Output: 一个带 `gap_closure: true` 的补洞计划，覆盖 monitor 图片路径、日志命令路径和对应 CAPT-04 回归测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VERIFICATION.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-02-PLAN.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md
@src-tauri/src/app_paths.rs
@src-tauri/src/clipboard/processor.rs
@src-tauri/src/clipboard/monitor.rs
@src-tauri/src/commands.rs
@src-tauri/src/app_paths_tests.rs
@src-tauri/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补 CAPT-04 回归测试，锁定日志路径与相对资源路径合同</name>
  <files>src-tauri/src/app_paths_tests.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-VERIFICATION.md, src-tauri/src/app_paths.rs, src-tauri/src/commands.rs, src-tauri/src/app_paths_tests.rs, src-tauri/src/lib.rs</read_first>
  <behavior>
    - Test 1: `test_app_paths_resolve_relative_asset_path_for_nested_imgs_assets` 用 temp-root 构造 `AppPaths`，断言 `resolve_relative_asset_path("imgs/nested/example.png")` 返回 `data/imgs/nested/example.png`。
    - Test 2: `test_app_paths_log_commands_follow_log_dir` 用 temp-root seed `logs/clipboard-app.log`，断言日志 helper 只读取和清空 `roots.log_root/clipboard-app.log`，不会访问用户 home 目录。
  </behavior>
  <action>在 `src-tauri/src/app_paths_tests.rs` 新增两个真实测试。第一个测试名固定为 `test_app_paths_resolve_relative_asset_path_for_nested_imgs_assets`：用 `create_temp_app_roots()` 和 `AppPaths::from_roots(...)` 构造路径对象，调用 `paths.resolve_relative_asset_path("imgs/nested/example.png")`，并断言结果等于 `roots.data_root.join("imgs").join("nested").join("example.png")`。第二个测试名固定为 `test_app_paths_log_commands_follow_log_dir`：同样构造 temp-root `AppPaths`，seed `logs/clipboard-app.log` 为可读文本，然后调用 `crate::commands::app_log_file_path(&paths)`、`crate::commands::read_log_content_in(&paths)`、`crate::commands::clear_log_file_in(&paths)`，断言 helper 路径严格等于 `roots.log_root.join("clipboard-app.log")`，读取结果等于 seed 文本，清空后文件内容变成空字符串。两个测试都必须只依赖 temp-root，不允许访问 `dirs::home_dir()`、`~/Library/Logs` 或任何真实用户目录。</action>
  <acceptance_criteria>
    - `rg -n "test_app_paths_resolve_relative_asset_path_for_nested_imgs_assets|test_app_paths_log_commands_follow_log_dir" src-tauri/src/app_paths_tests.rs` 有匹配结果。
    - `rg -n "app_log_file_path|read_log_content_in|clear_log_file_in|clipboard-app\\.log" src-tauri/src/app_paths_tests.rs` 有匹配结果。
    - `rg -n "home_dir|Library/Logs|dance\\.log" src-tauri/src/app_paths_tests.rs` 无匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_app_paths_log_commands_follow_log_dir -- --nocapture</automated>
  </verify>
  <done>CAPT-04 的 residual bypass 已经先被测试固定，后续实现不能再偷偷回到手写根目录。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 AppPaths 替换 monitor 与日志命令的剩余路径绕路</name>
  <files>src-tauri/src/clipboard/processor.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/commands.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-VERIFICATION.md, src-tauri/src/app_paths.rs, src-tauri/src/clipboard/processor.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/commands.rs, src-tauri/src/app_paths_tests.rs, src-tauri/src/lib.rs</read_first>
  <behavior>
    - Test 1: `ClipboardMonitor` 在回读 `imgs/...` 保存文件大小时，必须通过 `AppPaths::resolve_relative_asset_path()` 解析绝对路径；非相对输入仍允许直接走 `PathBuf::from(file_path)`。
    - Test 2: `get_log_content` 和 `clear_logs` 只通过 `state.paths.log_dir().join("clipboard-app.log")` 读写日志，保留“日志缺失返回空字符串 / 清理缺失日志 no-op”的现有行为。
  </behavior>
  <action>在 `src-tauri/src/clipboard/processor.rs` 给 `ContentProcessor` 保存 `Arc<AppPaths>`，并新增 `pub(crate) fn resolve_relative_asset_path(&self, relative: &str) -> Result<PathBuf>`，内部直接委托给 `self.paths.resolve_relative_asset_path(relative)`；保留 `imgs_dir()` 和现有图片处理逻辑不变。在 `src-tauri/src/clipboard/monitor.rs` 把 `get_saved_file_size` 改成实例方法：当 `file_path` 以 `imgs/` 开头时，调用 `self.processor.resolve_relative_asset_path(file_path)` 得到绝对路径，再读 metadata；否则继续使用 `PathBuf::from(file_path)`。删掉 monitor 中所有 `dirs::config_dir()` 和 `clipboard-app` 手写拼接。在 `src-tauri/src/commands.rs` 新增三个共享 helper，名称固定为 `pub(crate) fn app_log_file_path(paths: &AppPaths) -> PathBuf`、`pub(crate) fn read_log_content_in(paths: &AppPaths) -> Result<String, String>`、`pub(crate) fn clear_log_file_in(paths: &AppPaths) -> Result<(), String>`；其中 `app_log_file_path` 必须返回 `paths.log_dir().join("clipboard-app.log")`。然后把 Tauri 命令签名改成 `pub async fn get_log_content(state: State<'_, AppState>) -> Result<String, String>` 和 `pub async fn clear_logs(state: State<'_, AppState>) -> Result<(), String>`，命令体只委托给上述 helper。删除生产代码里所有 `dance.log`、`~/Library/Logs/com.dance.app` 和命令层 `dirs::home_dir()` / monitor 层 `dirs::config_dir()` 相关逻辑，但保持缺失日志时的返回语义不变。</action>
  <acceptance_criteria>
    - `rg -n "paths: Arc<AppPaths>|resolve_relative_asset_path" src-tauri/src/clipboard/processor.rs` 有匹配结果。
    - `rg -n "fn get_saved_file_size\\(&self|self\\.processor\\.resolve_relative_asset_path" src-tauri/src/clipboard/monitor.rs` 有匹配结果。
    - `rg -n "pub\\(crate\\) fn app_log_file_path|pub\\(crate\\) fn read_log_content_in|pub\\(crate\\) fn clear_log_file_in" src-tauri/src/commands.rs` 有匹配结果。
    - `rg -n "pub async fn get_log_content\\(state: State<'_, AppState>\\)|pub async fn clear_logs\\(state: State<'_, AppState>\\)" src-tauri/src/commands.rs` 有匹配结果。
    - `rg -n "dance\\.log|Library/Logs/com\\.dance\\.app|dirs::home_dir\\(|dirs::config_dir\\(" src-tauri/src/commands.rs src-tauri/src/clipboard/monitor.rs` 无匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_app_paths_resolve_relative_asset_path_for_nested_imgs_assets -- --nocapture</automated>
  </verify>
  <done>AppPaths 已经真正覆盖 verifier 点名的最后两条生产路径，CAPT-04 的路径权威闭环不再有旁路。</done>
</task>

</tasks>

<verification>
先执行 `cd src-tauri && cargo test test_app_paths_log_commands_follow_log_dir -- --nocapture`，再执行 `cd src-tauri && cargo test test_app_paths_resolve_relative_asset_path_for_nested_imgs_assets -- --nocapture`，最后补跑 `cd src-tauri && cargo test test_app_paths_migrate_legacy_roots -- --nocapture` 与 `cd src-tauri && cargo test --no-run`，确认 gap closure 没有破坏既有 CAPT-04 迁移链路和整体编译。
</verification>

<success_criteria>
`CAPT-04` 的剩余 blocker 已被压缩到零：monitor 图片 metadata 回读、日志读取/清理、以及对应回归测试都统一落在 `AppPaths` 权威根目录之下。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-06-SUMMARY.md`
</output>
