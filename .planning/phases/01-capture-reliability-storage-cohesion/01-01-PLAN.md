---
phase: 01-capture-reliability-storage-cohesion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/test_support.rs
  - src-tauri/src/app_paths_tests.rs
  - src-tauri/src/capture_runtime_tests.rs
  - src-tauri/src/capture_policy_tests.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - CAPT-01
  - CAPT-02
  - CAPT-03
  - CAPT-04
must_haves:
  truths:
    - 'Phase 1 的每个 requirement 都有可独立触发的自动化验证入口，执行时可以按 task 与 wave 连续抽样。'
    - '后续路径迁移、runtime 生命周期和 capture policy 改动都能在临时目录中验证，不会读写真实用户数据。'
    - '在进入手动 smoke 之前，Phase 1 的关键失败模式都能被命名明确的测试模块快速定位。'
  artifacts:
    - path: 'src-tauri/src/test_support.rs'
      provides: 'temp-root、fixture seed 与测试隔离辅助'
    - path: 'src-tauri/src/app_paths_tests.rs'
      provides: 'CAPT-04 路径与迁移测试脚手架'
    - path: 'src-tauri/src/capture_runtime_tests.rs'
      provides: 'CAPT-01/CAPT-02 运行时测试脚手架'
    - path: 'src-tauri/src/capture_policy_tests.rs'
      provides: 'CAPT-03 策略矩阵测试脚手架'
  key_links:
    - from: 'src-tauri/src/lib.rs'
      to: 'src-tauri/src/app_paths_tests.rs'
      via: 'cfg(test) module registration'
      pattern: 'mod app_paths_tests'
    - from: 'src-tauri/src/lib.rs'
      to: 'src-tauri/src/capture_runtime_tests.rs'
      via: 'cfg(test) module registration'
      pattern: 'mod capture_runtime_tests'
    - from: 'src-tauri/src/lib.rs'
      to: 'src-tauri/src/capture_policy_tests.rs'
      via: 'cfg(test) module registration'
      pattern: 'mod capture_policy_tests'
---

<objective>
先搭好 Phase 1 的验证骨架：测试辅助、测试文件、模块注册和命名约定全部落盘。

Purpose: 这一步只解决“怎么验证”，不抢跑实现细节。后续 runtime、paths、policy 三个计划都直接在这里补充真实断言。
Output: `test_support.rs`、三个 Phase 1 测试文件，以及 `lib.rs` 中对应的测试模块注册。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md
@src-tauri/src/lib.rs
@src-tauri/src/state_tests.rs
@src-tauri/src/integration_tests.rs
@src-tauri/src/performance_tests.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 hermetic test support 与 Phase 1 测试模块注册</name>
  <files>src-tauri/src/test_support.rs, src-tauri/src/lib.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md, src-tauri/src/lib.rs, src-tauri/src/state_tests.rs, src-tauri/src/integration_tests.rs</read_first>
  <action>新增 `src-tauri/src/test_support.rs`，定义一个明确的测试根目录辅助，例如 `pub struct TestAppRoots { temp_dir: TempDir, config_root: PathBuf, data_root: PathBuf, cache_root: PathBuf, log_root: PathBuf }`，并暴露这些 helper：`create_temp_app_roots() -> TestAppRoots`、`sqlite_url(file_name: &str) -> String`、`seed_file(relative: &str, bytes: &[u8]) -> PathBuf`、`create_dir(relative: &str) -> PathBuf`。目录布局固定为 `config/`、`data/`、`cache/`、`logs/` 四个一级目录。然后在 `src-tauri/src/lib.rs` 追加 `#[cfg(test)] mod test_support;`、`#[cfg(test)] mod app_paths_tests;`、`#[cfg(test)] mod capture_runtime_tests;`、`#[cfg(test)] mod capture_policy_tests;`，位置保持和现有 `state_tests` / `integration_tests` 一致，禁止把这些模块注册到生产构建路径里。</action>
  <acceptance_criteria>
    - `rg -n "pub struct TestAppRoots|create_temp_app_roots|sqlite_url|seed_file|create_dir" src-tauri/src/test_support.rs` 有匹配结果。
    - `rg -n "mod test_support|mod app_paths_tests|mod capture_runtime_tests|mod capture_policy_tests" src-tauri/src/lib.rs` 有 4 个匹配结果。
    - `rg -n "TempDir|config_root|data_root|cache_root|log_root" src-tauri/src/test_support.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test --no-run</automated>
  </verify>
  <done>Phase 1 的测试辅助和模块注册已存在，后续计划可以直接往对应测试文件补内容。</done>
</task>

<task type="auto">
  <name>Task 2: 生成 Phase 1 三类测试脚手架与固定测试名</name>
  <files>src-tauri/src/app_paths_tests.rs, src-tauri/src/capture_runtime_tests.rs, src-tauri/src/capture_policy_tests.rs, src-tauri/src/test_support.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md, src-tauri/src/test_support.rs, src-tauri/src/state_tests.rs</read_first>
  <action>新增三个测试文件，并固定这些测试名供后续计划实现：`src-tauri/src/app_paths_tests.rs` 包含可执行的 `test_app_paths_temp_roots_are_isolated`，以及 `#[ignore = "implemented in 01-02"]` 的 `test_app_paths_migrate_legacy_roots`；`src-tauri/src/capture_runtime_tests.rs` 包含 `#[ignore = "implemented in 01-03"]` 的 `test_capture_runtime_stop_cancels_tasks`、`test_capture_runtime_single_worker_and_suppression`、`test_capture_runtime_restart_is_single_owner`；`src-tauri/src/capture_policy_tests.rs` 包含 `#[ignore = "implemented in 01-04"]` 的 `test_capture_policy_marker_matrix`、`test_capture_policy_current_only_is_non_persistent_in_v1`。`test_app_paths_temp_roots_are_isolated` 必须真实执行并断言 `config/`、`data/`、`cache/`、`logs/` 都在 temp 根目录下且互不相同；其他测试先只保留空体和 ignore 注解，确保文件可编译、命名固定、后续实现无需再重命名。</action>
  <acceptance_criteria>
    - `rg -n "test_app_paths_temp_roots_are_isolated|test_app_paths_migrate_legacy_roots" src-tauri/src/app_paths_tests.rs` 有 2 个测试名。
    - `rg -n "test_capture_runtime_stop_cancels_tasks|test_capture_runtime_single_worker_and_suppression|test_capture_runtime_restart_is_single_owner" src-tauri/src/capture_runtime_tests.rs` 有 3 个测试名。
    - `rg -n "test_capture_policy_marker_matrix|test_capture_policy_current_only_is_non_persistent_in_v1" src-tauri/src/capture_policy_tests.rs` 有 2 个测试名。
    - `rg -n "#\\[ignore = \"implemented in 01-02\"\\]|#\\[ignore = \"implemented in 01-03\"\\]|#\\[ignore = \"implemented in 01-04\"\\]" src-tauri/src/app_paths_tests.rs src-tauri/src/capture_runtime_tests.rs src-tauri/src/capture_policy_tests.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_app_paths_temp_roots_are_isolated -- --nocapture</automated>
  </verify>
  <done>Phase 1 的验证入口已经固定，后续计划只负责把 ignore scaffold 替换成真实实现与断言。</done>
</task>

</tasks>

<verification>
执行 `cd src-tauri && cargo test --no-run` 确认测试模块已注册且可编译，再执行 `cd src-tauri && cargo test test_app_paths_temp_roots_are_isolated -- --nocapture` 验证 temp-root scaffold 正常工作。
</verification>

<success_criteria>
Phase 1 的 validation/test scaffolding 已经就位，`CAPT-01..04` 各自有固定测试文件和测试名，且至少有一个 temp-root 隔离测试真实可运行。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-01-SUMMARY.md`
</output>
