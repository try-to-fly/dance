---
phase: 01-capture-reliability-storage-cohesion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/app_paths.rs
  - src-tauri/src/test_support.rs
  - src-tauri/src/database/mod.rs
  - src-tauri/src/config/mod.rs
  - src-tauri/src/clipboard/processor.rs
  - src-tauri/src/utils/app_icon_extractor.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/state.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/app_paths_tests.rs
autonomous: true
requirements:
  - CAPT-04
must_haves:
  truths:
    - '应用只通过一套权威路径解析配置、数据库、图片资源、图标缓存和日志目录。'
    - '升级后旧的 dance 与 clipboard-app 根目录不会因为路径切换而丢失历史、配置或图片资源。'
    - '测试可以在临时根目录中运行，而不是读写开发者真实的 ~/Library 目录。'
  artifacts:
    - path: 'src-tauri/src/app_paths.rs'
      provides: '统一的 AppPaths 权威路径与遗留迁移入口'
      exports:
        - 'AppPaths'
    - path: 'src-tauri/src/test_support.rs'
      provides: '临时路径注入与 hermetic 测试辅助'
    - path: 'src-tauri/src/app_paths_tests.rs'
      provides: 'CAPT-04 路径与迁移自动化验证'
  key_links:
    - from: 'src-tauri/src/app_paths.rs'
      to: 'src-tauri/src/database/mod.rs'
      via: 'history_db_path()'
      pattern: 'history_db_path'
    - from: 'src-tauri/src/app_paths.rs'
      to: 'src-tauri/src/clipboard/processor.rs'
      via: 'image_assets_dir()'
      pattern: 'image_assets_dir'
    - from: 'src-tauri/src/app_paths.rs'
      to: 'src-tauri/src/commands.rs'
      via: 'resolve_relative_asset_path()'
      pattern: 'resolve_relative_asset_path'
---

<objective>
建立 Phase 1 的存储权威层与可注入测试根目录，让历史、配置、图片资源、图标缓存和日志都走同一套本地生命周期。

Purpose: 先解决路径漂移和真实用户目录泄漏问题，后续运行时、去重和策略改造才能在可重复环境里验证。
Output: `AppPaths` 模块、temp-root 测试辅助、遗留根目录迁移逻辑，以及所有现有路径消费者的统一接入点。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md
@src-tauri/src/lib.rs
@src-tauri/src/database/mod.rs
@src-tauri/src/config/mod.rs
@src-tauri/src/clipboard/processor.rs
@src-tauri/src/utils/app_icon_extractor.rs
@src-tauri/src/commands.rs
@src-tauri/src/state.rs

<interfaces>
From `src-tauri/src/database/mod.rs`:

```rust
pub struct Database {
    pool: Pool<Sqlite>,
}

pub async fn new() -> Result<Self>;
```

From `src-tauri/src/config/mod.rs`:

```rust
pub struct ConfigManager {
    config_path: PathBuf,
    pub config: AppConfig,
}

pub async fn new() -> Result<Self>;
```

From `src-tauri/src/clipboard/processor.rs`:

```rust
pub struct ContentProcessor {
    imgs_dir: PathBuf,
}

pub fn new() -> Result<Self>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 AppPaths 权威路径契约与 temp-root 测试辅助</name>
  <files>src-tauri/src/app_paths.rs, src-tauri/src/test_support.rs, src-tauri/src/database/mod.rs, src-tauri/src/config/mod.rs, src-tauri/src/clipboard/processor.rs, src-tauri/src/lib.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, .planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md, src-tauri/src/lib.rs, src-tauri/src/database/mod.rs, src-tauri/src/config/mod.rs, src-tauri/src/clipboard/processor.rs</read_first>
  <behavior>
    - Test 1: `AppPaths::from_test_root(...)` 生成固定布局 `config/config.json`、`data/clipboard.db`、`data/imgs`、`cache/icons`、`logs/`。
    - Test 2: `Database::new_in(...)`、`ConfigManager::new_in(...)`、`ContentProcessor::new_in(...)` 全部复用同一个 `AppPaths`，不再直接调用 `dirs::config_dir()`。
  </behavior>
  <action>新增 `src-tauri/src/app_paths.rs`，定义 `pub struct AppPaths`，并提供这些明确 helper：`config_file_path() -> PathBuf`、`history_db_path() -> PathBuf`、`image_assets_dir() -> PathBuf`、`icon_cache_dir() -> PathBuf`、`log_dir() -> PathBuf`、`migration_marker_path() -> PathBuf`、`legacy_roots() -> [PathBuf; 2]`、`resolve_relative_asset_path(relative: &str) -> Result<PathBuf>`。运行态路径必须来自 Tauri `PathResolver`，具体映射固定为：配置文件=`app_config_dir()/config.json`，数据库=`app_data_dir()/clipboard.db`，历史图片=`app_data_dir()/imgs`，图标缓存=`app_cache_dir()/icons`，日志=`app_log_dir()`。同时新增 `src-tauri/src/test_support.rs`，提供 `create_temp_app_paths()` 或等价 helper，返回 `TempDir` 与可注入的 `AppPaths`。把 `Database`、`ConfigManager`、`ContentProcessor` 分别扩展为 `new_in(paths: Arc<AppPaths>)` 或等价注入构造，保留现有 `new()` 作为委托包装。`src-tauri/src/lib.rs` 中 `AppState::new()` 的调用要先构造 `AppPaths` 再传入，禁止继续在运行时构造阶段散落路径字符串。</action>
  <acceptance_criteria>
    - `rg -n "pub struct AppPaths|config_file_path|history_db_path|image_assets_dir|icon_cache_dir|migration_marker_path|resolve_relative_asset_path" src-tauri/src/app_paths.rs` 至少匹配 7 个符号。
    - `rg -n "new_in\\(" src-tauri/src/database/mod.rs src-tauri/src/config/mod.rs src-tauri/src/clipboard/processor.rs` 每个文件都至少有 1 个注入构造。
    - `rg -n "create_temp_app_paths|TempDir" src-tauri/src/test_support.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_app_paths_temp_roots_are_isolated -- --nocapture</automated>
  </verify>
  <done>路径契约和临时根目录注入接口已存在，后续计划不需要再直接拼接 `dance` 或 `clipboard-app` 根路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 统一所有现有存储消费者并实现显式遗留迁移</name>
  <files>src-tauri/src/app_paths.rs, src-tauri/src/test_support.rs, src-tauri/src/utils/app_icon_extractor.rs, src-tauri/src/commands.rs, src-tauri/src/state.rs, src-tauri/src/app_paths_tests.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src-tauri/src/app_paths.rs, src-tauri/src/test_support.rs, src-tauri/src/utils/app_icon_extractor.rs, src-tauri/src/commands.rs, src-tauri/src/state.rs</read_first>
  <behavior>
    - Test 1: 当旧目录存在 `~/.../dance/clipboard.db`、`~/.../dance/config.json`、`~/.../clipboard-app/imgs/`、`~/.../clipboard-app/icons/` 且新目录为空时，迁移后新目录获得这些资产。
    - Test 2: 当新目录已有目标文件时，迁移保持新目录优先，遗留目录不被覆盖，并写出幂等迁移标记。
  </behavior>
  <action>在 `AppPaths` 中实现显式迁移入口 `migrate_legacy_roots()`。遗留输入必须只认这两组路径：`dirs::config_dir()/dance` 与 `dirs::config_dir()/clipboard-app`。迁移规则固定为：`dance/config.json -> app_config_dir()/config.json`，`dance/clipboard.db -> app_data_dir()/clipboard.db`，`clipboard-app/imgs/* -> app_data_dir()/imgs/*`，`clipboard-app/icons/* -> app_cache_dir()/icons/*`。迁移完成后写入 `app_data_dir()/migrations/capt04-storage-roots.json`，内容至少包含 `version: "capt04-storage-roots"`, `migrated_from`, `completed_at`。把 `AppIconExtractor`、`open_file_with_system`、`get_image_url`、`AppState::get_cache_statistics`、`AppState` 内图片/数据库辅助路径全部改成走 `AppPaths` 或由注入对象暴露的 getter；不要再在这些文件里直接调用 `dirs::config_dir()` 或手写 `"dance"` / `"clipboard-app"`。新增 `src-tauri/src/app_paths_tests.rs`，测试名使用 `test_app_paths_temp_roots_are_isolated` 与 `test_app_paths_migrate_legacy_roots`，并让 `src-tauri/src/lib.rs` 在创建 `AppState` 前先执行迁移。</action>
  <acceptance_criteria>
    - `rg -n "capt04-storage-roots|migrate_legacy_roots|completed_at" src-tauri/src/app_paths.rs src-tauri/src/app_paths_tests.rs` 有匹配结果。
    - `rg -n "dirs::config_dir\\(" src-tauri/src/database/mod.rs src-tauri/src/config/mod.rs src-tauri/src/clipboard/processor.rs src-tauri/src/utils/app_icon_extractor.rs src-tauri/src/commands.rs src-tauri/src/state.rs` 无匹配结果。
    - `rg -n "\"dance\"|\"clipboard-app\"" src-tauri/src/database/mod.rs src-tauri/src/config/mod.rs src-tauri/src/clipboard/processor.rs src-tauri/src/utils/app_icon_extractor.rs src-tauri/src/commands.rs src-tauri/src/state.rs` 仅允许出现在迁移兼容代码中，不能再出现在常规路径解析逻辑中。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_app_paths_migrate_legacy_roots -- --nocapture</automated>
  </verify>
  <done>所有本地持久化与资源路径都由 `AppPaths` 统一分发，CAPT-04 所需的迁移与 hermetic 测试基线已经到位。</done>
</task>

</tasks>

<verification>
先运行 `cd src-tauri && cargo test test_app_paths_temp_roots_are_isolated -- --nocapture`，再运行 `cd src-tauri && cargo test test_app_paths_migrate_legacy_roots -- --nocapture`。若任何 targeted test 失败，再补跑 `cd src-tauri && cargo test` 确认没有遗漏的调用点仍在读写真实用户目录。
</verification>

<success_criteria>
`AppPaths` 成为唯一的本地路径权威，遗留目录迁移可重复执行，且路径相关测试完全在临时目录中通过。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-01-SUMMARY.md`
</output>
