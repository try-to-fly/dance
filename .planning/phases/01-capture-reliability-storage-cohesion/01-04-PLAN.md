---
phase: 01-capture-reliability-storage-cohesion
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/stores/clipboardStore.ts
  - src/stores/clipboardStore.test.ts
  - src/components/ClipboardMenuHandler.tsx
  - src/components/DetailView/ContentRenderers/JsonRenderer.tsx
  - src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx
  - src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx
  - src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx
  - src/components/LogViewer/LogViewer.tsx
autonomous: true
requirements:
  - CAPT-02
must_haves:
  truths:
    - '所有当前 app 内文本复制入口都统一走 backend-owned `copy_to_clipboard` 合同。'
    - '前端不再直接调用 `writeText` 或 `navigator.clipboard.writeText`，自写 suppression 不会被 UI 分支绕过。'
    - '复制入口 contract 有稳定前端测试覆盖，后续回退能在执行阶段被快速发现。'
  artifacts:
    - path: 'src/stores/clipboardStore.ts'
      provides: '共享 backend copy helper 与 store-level routing'
    - path: 'src/stores/clipboardStore.test.ts'
      provides: 'store copy-routing contract tests'
    - path: 'src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx'
      provides: 'renderer copy-routing contract tests'
    - path: 'src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx'
      provides: 'renderer copy-routing contract tests'
  key_links:
    - from: 'src/stores/clipboardStore.ts'
      to: 'src/components/ClipboardMenuHandler.tsx'
      via: 'copyToClipboard()'
      pattern: 'copyToClipboard'
    - from: 'src/stores/clipboardStore.ts'
      to: 'src/components/DetailView/ContentRenderers/JsonRenderer.tsx'
      via: 'shared backend copy helper'
      pattern: 'copy_to_clipboard'
    - from: 'src/stores/clipboardStore.ts'
      to: 'src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx'
      via: 'shared backend copy helper'
      pattern: 'copy_to_clipboard'
---

<objective>
统一所有现有前端 copy 入口到 backend-owned `copy_to_clipboard` 合同，完成 CAPT-02 的 UI 覆盖闭环。

Purpose: 让 app 内所有自写 copy flow 都经过 01-03 已建立的后端 suppression contract，不再被组件级直写系统剪贴板绕过。
Output: 共享 backend copy helper、迁移后的 UI copy 入口，以及稳定的前端 contract tests。

Execution boundary: 先收敛 store/menu 级别的共享 copy helper 和 contract test；只有 Task 1 通过后，Task 2 才开始 sweep renderer 与 log viewer 的剩余入口。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md
@src-tauri/src/commands.rs
@src/stores/clipboardStore.ts
@src/stores/clipboardStore.test.ts
@src/components/ClipboardMenuHandler.tsx
@src/components/DetailView/ContentRenderers/JsonRenderer.tsx
@src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx
@src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx
@src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx
@src/components/LogViewer/LogViewer.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收敛共享 backend copy helper 与 store/menu 合同</name>
  <files>src/stores/clipboardStore.ts, src/stores/clipboardStore.test.ts, src/components/ClipboardMenuHandler.tsx</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src-tauri/src/commands.rs, src/stores/clipboardStore.ts, src/stores/clipboardStore.test.ts, src/components/ClipboardMenuHandler.tsx</read_first>
  <behavior>
    - Test 1: `copyToClipboard()` 统一落到 `invoke('copy_to_clipboard', { content })`。
    - Test 2: `ClipboardMenuHandler` 不再直接 `navigator.clipboard.writeText`。
    - Test 3: `clipboardStore.test.ts` 明确覆盖 invoke routing 与不直接写系统剪贴板的 contract。
  </behavior>
  <action>在 `src/stores/clipboardStore.ts` 中收敛一个共享 backend copy helper，例如 `copyToClipboard(content)` 或等价封装，最终调用必须落到 `invoke('copy_to_clipboard', { content })`。`src/components/ClipboardMenuHandler.tsx` 改为只消费这个共享 helper 或 store 路由，不再直接调用 `navigator.clipboard.writeText`。同步更新 `src/stores/clipboardStore.test.ts`，显式断言 store 层复制入口走 backend route，且不会再直接调用系统写剪贴板 API。</action>
  <acceptance_criteria>
    - `rg -n "invoke\\('copy_to_clipboard', \\{ content \\}\\)" src/stores/clipboardStore.ts` 有匹配结果。
    - `rg -n "writeText|navigator\\.clipboard\\.writeText" src/stores/clipboardStore.ts src/components/ClipboardMenuHandler.tsx` 无匹配结果。
    - `rg -n "copy_to_clipboard|invoke" src/stores/clipboardStore.test.ts` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/stores/clipboardStore.test.ts</automated>
  </verify>
  <done>store 与菜单级复制入口已经统一接到后端 copy contract，上层组件不再直接写系统剪贴板。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sweep renderer 与 log viewer 的剩余 copy 入口并补 contract tests</name>
  <files>src/components/DetailView/ContentRenderers/JsonRenderer.tsx, src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx, src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx, src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx, src/components/LogViewer/LogViewer.tsx</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src/stores/clipboardStore.ts, src/components/DetailView/ContentRenderers/JsonRenderer.tsx, src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx, src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx, src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx, src/components/LogViewer/LogViewer.tsx</read_first>
  <behavior>
    - Test 1: `JsonRenderer`、`UnifiedTextRenderer`、`LogViewer` 都改为调用共享 backend copy 路由。
    - Test 2: 这些组件中不再保留任何 `writeText` 或 `navigator.clipboard.writeText`。
    - Test 3: `JsonRenderer.test.tsx` 与 `UnifiedTextRenderer.test.tsx` 明确覆盖 backend copy-routing contract。
  </behavior>
  <action>把 `src/components/DetailView/ContentRenderers/JsonRenderer.tsx`、`src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx` 和 `src/components/LogViewer/LogViewer.tsx` 中所有当前直接 `writeText` 或 `navigator.clipboard.writeText` 的路径都改成走 Task 1 提供的共享 backend copy helper。同步更新 `src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx` 与 `src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx`，显式断言这些入口不再直接写系统剪贴板，而是调用 backend copy 路由。</action>
  <acceptance_criteria>
    - `rg -n "writeText|navigator\\.clipboard\\.writeText" src/components/DetailView/ContentRenderers/JsonRenderer.tsx src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx src/components/LogViewer/LogViewer.tsx` 无匹配结果。
    - `rg -n "copy_to_clipboard|invoke" src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx` 有匹配结果。
    - `rg -n "copyToClipboard|copy_to_clipboard" src/components/DetailView/ContentRenderers/JsonRenderer.tsx src/components/DetailView/ContentRenderers/UnifiedTextRenderer.tsx src/components/LogViewer/LogViewer.tsx` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx</automated>
  </verify>
  <done>CAPT-02 的前端 copy-routing 合同已经覆盖到所有当前 app 内复制入口，后续执行不会再把自写路径留在前端隐式处理。</done>
</task>

</tasks>

<verification>
先执行 Task 1 的 `pnpm test -- src/stores/clipboardStore.test.ts`。该命令通过后，才进入 Task 2，并执行 `pnpm test -- src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx`。最后补跑一次 `pnpm test -- src/stores/clipboardStore.test.ts src/components/DetailView/ContentRenderers/JsonRenderer.test.tsx src/components/DetailView/ContentRenderers/UnifiedTextRenderer.test.tsx`，确认前端 copy-routing contract 全部通过。
</verification>

<success_criteria>
CAPT-02 的 UI copy path 已全部接入 backend contract：所有当前 app 内自写复制入口都会经过 suppression，不再制造隐式回流。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-04-SUMMARY.md`
</output>
