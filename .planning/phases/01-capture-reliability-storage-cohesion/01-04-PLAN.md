---
phase: 01-capture-reliability-storage-cohesion
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-03
files_modified:
  - src-tauri/src/capture/mod.rs
  - src-tauri/src/capture/policy.rs
  - src-tauri/src/capture/macos_markers.rs
  - src-tauri/src/clipboard/monitor.rs
  - src-tauri/src/capture_policy_tests.rs
autonomous: true
requirements:
  - CAPT-03
must_haves:
  truths:
    - '瞬态、concealed、remote、自生成等不应持久化的事件会在入库前被拦截。'
    - 'CAPT-03 的判断先看 pasteboard marker 和来源元数据，再决定是否做内容检测。'
    - '非 macOS 平台保持 no-op adapter，不会因为缺 marker API 而破坏现有采集。'
  artifacts:
    - path: 'src-tauri/src/capture/policy.rs'
      provides: 'marker-first CapturePolicy'
    - path: 'src-tauri/src/capture/macos_markers.rs'
      provides: 'macOS pasteboard marker adapter'
    - path: 'src-tauri/src/capture_policy_tests.rs'
      provides: 'CAPT-03 自动化验证'
  key_links:
    - from: 'src-tauri/src/capture/macos_markers.rs'
      to: 'src-tauri/src/capture/policy.rs'
      via: 'PasteboardMarkers'
      pattern: 'PasteboardMarkers'
    - from: 'src-tauri/src/capture/policy.rs'
      to: 'src-tauri/src/clipboard/monitor.rs'
      via: 'decide_capture'
      pattern: 'decide_capture'
    - from: 'src-tauri/src/clipboard/monitor.rs'
      to: 'src-tauri/src/capture/runtime.rs'
      via: 'only Persist reaches save loop'
      pattern: 'CaptureDisposition::Persist'
---

<objective>
把 CAPT-03 固定成 marker-first capture policy，并在 monitor 中先判策略再做内容检测和持久化。

Purpose: 这一步只解决“不该进历史的事件如何被挡掉”，不扩展到 Phase 2 的 subtype 分析或预览语义。
Output: `CapturePolicy`、macOS marker adapter、monitor 接线，以及 CAPT-03 的真实自动化测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md
@src-tauri/src/capture/runtime.rs
@src-tauri/src/clipboard/monitor.rs
@src-tauri/src/capture_policy_tests.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 marker-first CapturePolicy 并落地 CAPT-03 策略矩阵</name>
  <files>src-tauri/src/capture/mod.rs, src-tauri/src/capture/policy.rs, src-tauri/src/capture_policy_tests.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, .planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md, src-tauri/src/capture_policy_tests.rs, src-tauri/src/clipboard/monitor.rs</read_first>
  <behavior>
    - Test 1: `transient`、`concealed`、`remote`、`self_generated`、`excluded_app`、`oversize_text` 都返回 `CaptureDisposition::Skip`。
    - Test 2: `auto_generated` 返回 `CaptureDisposition::CurrentOnly`，且 Phase 1 中明确解释为“不入库”。
    - Test 3: 普通文本或图片且未命中任何 marker/来源规则时返回 `CaptureDisposition::Persist`。
  </behavior>
  <action>新增 `src-tauri/src/capture/policy.rs`，定义 `CaptureDisposition::{Persist, CurrentOnly, Skip}`、`PasteboardMarkers { is_transient: bool, is_concealed: bool, is_auto_generated: bool, is_remote: bool, source_bundle_id: Option<String> }`，以及纯函数 `decide_capture(markers: &PasteboardMarkers, self_generated: bool, excluded_app: bool, text_size_valid: bool) -> CaptureDisposition`。规则写死为：`self_generated` 或 `is_transient` 或 `is_concealed` 或 `is_remote` 或 `excluded_app` 或 `text_size_valid == false` 时返回 `Skip`；`is_auto_generated` 返回 `CurrentOnly`；其他返回 `Persist`。把 `src-tauri/src/capture_policy_tests.rs` 里的 `test_capture_policy_marker_matrix` 和 `test_capture_policy_current_only_is_non_persistent_in_v1` 去掉 ignore 并实现真实断言，禁止在这里加入任何 Phase 2 的 subtype 识别逻辑。</action>
  <acceptance_criteria>
    - `rg -n "CaptureDisposition|PasteboardMarkers|decide_capture" src-tauri/src/capture/policy.rs` 有匹配结果。
    - `rg -n "CurrentOnly|Skip|Persist" src-tauri/src/capture/policy.rs src-tauri/src/capture_policy_tests.rs` 有匹配结果。
    - `rg -n "#\\[ignore = \"implemented in 01-04\"\\]" src-tauri/src/capture_policy_tests.rs` 无匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_capture_policy_marker_matrix -- --nocapture</automated>
  </verify>
  <done>CAPT-03 的策略规则已经被提炼成纯函数和自动化测试，不再散落在 monitor 的启发式分支里。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 macOS marker adapter，并在 monitor 中先判策略再做检测</name>
  <files>src-tauri/src/capture/mod.rs, src-tauri/src/capture/macos_markers.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/capture_policy_tests.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src-tauri/src/capture/policy.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/capture/runtime.rs, src-tauri/src/capture_policy_tests.rs</read_first>
  <behavior>
    - Test 1: 非 macOS 构建返回全 false marker，正常内容仍可 `Persist`。
    - Test 2: monitor 命中 `Skip` 或 `CurrentOnly` 时不会把条目送进 runtime save loop。
    - Test 3: marker 采集发生在 `ContentDetector::detect(...)` 之前。
  </behavior>
  <action>新增 `src-tauri/src/capture/macos_markers.rs`。在 macOS 下用已有 `cocoa` / `objc` 读取这些明确 marker：`org.nspasteboard.TransientType`、`org.nspasteboard.ConcealedType`、`org.nspasteboard.AutoGeneratedType`、`org.nspasteboard.source`、`com.apple.is-remote-clipboard`。非 macOS 下返回 `PasteboardMarkers` 的 no-op 版本：所有布尔值为 `false`，`source_bundle_id = None`。修改 `src-tauri/src/clipboard/monitor.rs`，顺序必须是：读取 marker -> 结合排除应用和文本大小调用 `decide_capture(...)` -> 只有 `Persist` 才继续走 `ContentDetector::detect(...)` 与 `tx.send(entry)`。`CurrentOnly` 在 Phase 1 中只更新 runtime 内存去重态，不写数据库；而且它必须调用 Plan 03 已定义的统一键合同，例如 `remember_observed_hash(content_hash.clone())`，这里的 `content_hash` 必须与 `register_suppression_key(content_hash, ttl_ms)` 使用完全相同的 SHA256 十六进制字符串。这样 `CurrentOnly`、普通持久化和自写 suppression 三条路径共享同一套 dedupe key。不要在这里重复实现 runtime 的 suppression registry；直接消费 Plan 03 提供的 `remember_observed_hash(...)` / `register_suppression_key(...)` 接口。</action>
  <acceptance_criteria>
    - `rg -n "org\\.nspasteboard\\.TransientType|org\\.nspasteboard\\.ConcealedType|org\\.nspasteboard\\.AutoGeneratedType|org\\.nspasteboard\\.source|com\\.apple\\.is-remote-clipboard" src-tauri/src/capture/macos_markers.rs` 有匹配结果。
    - `rg -n "decide_capture|CaptureDisposition::Persist|CaptureDisposition::CurrentOnly|CaptureDisposition::Skip" src-tauri/src/clipboard/monitor.rs` 有匹配结果。
    - `rg -n "remember_observed_hash|register_suppression_key|content_hash" src-tauri/src/clipboard/monitor.rs src-tauri/src/capture/runtime.rs` 有匹配结果。
    - `rg -n "ContentDetector::detect" src-tauri/src/clipboard/monitor.rs` 有匹配结果，且前面已有 marker/policy 判定分支。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_capture_policy_current_only_is_non_persistent_in_v1 -- --nocapture</automated>
  </verify>
  <done>CAPT-03 已按 marker-first 方式接入实际 monitor 流程，平台能力边界明确且可验证。</done>
</task>

</tasks>

<verification>
先执行 `cd src-tauri && cargo test test_capture_policy_marker_matrix -- --nocapture`，再执行 `cd src-tauri && cargo test test_capture_policy_current_only_is_non_persistent_in_v1 -- --nocapture`。如果本机是 macOS，再补一次手动 smoke，确认瞬态、concealed、remote 和正常内容的入库行为符合预期。
</verification>

<success_criteria>
CAPT-03 已被严格限定在 capture policy 范围内完成：先看 marker，再决定是否检测和持久化，不提前展开 Phase 2 的分析逻辑。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-04-SUMMARY.md`
</output>
