---
phase: 01-capture-reliability-storage-cohesion
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src-tauri/src/capture/mod.rs
  - src-tauri/src/capture/policy.rs
  - src-tauri/src/capture/macos_markers.rs
  - src-tauri/src/clipboard/monitor.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/capture_policy_tests.rs
autonomous: true
requirements:
  - CAPT-03
must_haves:
  truths:
    - '瞬态、concealed、remote clipboard 等不应持久化的事件会在进入数据库前被过滤。'
    - 'CAPT-03 的规则来自 pasteboard marker 与来源元数据，而不是只靠文本内容猜测。'
    - '非 macOS 平台保持 capability-based no-op，而不会因为缺少 marker API 破坏现有采集。'
  artifacts:
    - path: 'src-tauri/src/capture/policy.rs'
      provides: 'CaptureDisposition 与 marker-first policy'
      exports:
        - 'CaptureDisposition'
        - 'PasteboardMarkers'
    - path: 'src-tauri/src/capture/macos_markers.rs'
      provides: 'macOS pasteboard marker adapter'
    - path: 'src-tauri/src/capture_policy_tests.rs'
      provides: 'CAPT-03 marker matrix 自动化验证'
  key_links:
    - from: 'src-tauri/src/capture/macos_markers.rs'
      to: 'src-tauri/src/capture/policy.rs'
      via: 'PasteboardMarkers'
      pattern: 'PasteboardMarkers'
    - from: 'src-tauri/src/capture/policy.rs'
      to: 'src-tauri/src/clipboard/monitor.rs'
      via: 'decide_capture(...)'
      pattern: 'decide_capture'
    - from: 'src-tauri/src/clipboard/monitor.rs'
      to: 'src-tauri/src/capture/runtime.rs'
      via: 'only Persist events reach runtime save loop'
      pattern: 'CaptureDisposition::Persist'
---

<objective>
把 CAPT-03 固定成 marker-first 的采集策略，而不是继续堆内容字符串 heuristics。

Purpose: 用户需要的是“不该进历史的剪贴板事件被挡在持久化之前”，尤其是瞬态、concealed、remote 和自动生成内容。
Output: `CapturePolicy`、macOS marker adapter、monitor 集成点，以及 CAPT-03 的自动化策略矩阵。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md
@src-tauri/src/capture/runtime.rs
@src-tauri/src/clipboard/monitor.rs
@src-tauri/src/lib.rs

<interfaces>
Recommended policy contract from research:

```rust
pub enum CaptureDisposition {
    Persist,
    CurrentOnly,
    Skip,
}

pub struct PasteboardMarkers {
    pub is_transient: bool,
    pub is_concealed: bool,
    pub is_auto_generated: bool,
    pub is_remote: bool,
    pub source_bundle_id: Option<String>,
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 marker-first CapturePolicy 并建立策略矩阵测试</name>
  <files>src-tauri/src/capture/mod.rs, src-tauri/src/capture/policy.rs, src-tauri/src/capture_policy_tests.rs, src-tauri/src/lib.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, .planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md, src-tauri/src/capture/runtime.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/lib.rs</read_first>
  <behavior>
    - Test 1: `transient`、`concealed`、`remote`、`self_generated`、`excluded_app`、`oversize_text` 都返回 `CaptureDisposition::Skip`。
    - Test 2: `auto_generated` 返回 `CaptureDisposition::CurrentOnly`，并在 Phase 1 中明确映射为“不入库”行为。
    - Test 3: 普通文本或图片且未命中任何 marker/排除规则时返回 `CaptureDisposition::Persist`。
  </behavior>
  <action>新增 `src-tauri/src/capture/policy.rs`，定义 `CaptureDisposition::{Persist, CurrentOnly, Skip}`、`PasteboardMarkers`，以及纯函数 `decide_capture(markers: &PasteboardMarkers, self_generated: bool, excluded_app: bool, text_size_valid: bool) -> CaptureDisposition`。规则必须写死为：`self_generated == true` -> `Skip`；`is_transient || is_concealed || is_remote` -> `Skip`；`excluded_app == true` -> `Skip`；`text_size_valid == false` -> `Skip`；`is_auto_generated == true` -> `CurrentOnly`；其余 -> `Persist`。同时在计划文本中就明确 Phase 1 的落地语义：`CurrentOnly` 暂时映射为“更新内存态/last_hash，但不发往 save loop、不写数据库”，不要把这个决定留在实现时猜。新增 `src-tauri/src/capture_policy_tests.rs` 并在 `src-tauri/src/lib.rs` 注册，至少包含 `test_capture_policy_marker_matrix` 与 `test_capture_policy_current_only_is_non_persistent_in_v1`。</action>
  <acceptance_criteria>
    - `rg -n "CaptureDisposition|PasteboardMarkers|decide_capture" src-tauri/src/capture/policy.rs` 有匹配结果。
    - `rg -n "test_capture_policy_marker_matrix|test_capture_policy_current_only_is_non_persistent_in_v1" src-tauri/src/capture_policy_tests.rs` 有匹配结果。
    - `rg -n "CurrentOnly" src-tauri/src/capture/policy.rs src-tauri/src/capture_policy_tests.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_capture_policy_marker_matrix -- --nocapture</automated>
  </verify>
  <done>CAPT-03 的判定规则已经被提炼成纯函数和测试矩阵，而不是散落在 monitor 内部的条件分支。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 macOS marker adapter 并在 monitor 中先判策略再做检测/持久化</name>
  <files>src-tauri/src/capture/mod.rs, src-tauri/src/capture/macos_markers.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/capture_policy_tests.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src-tauri/src/capture/policy.rs, src-tauri/src/clipboard/monitor.rs, src-tauri/src/capture/runtime.rs</read_first>
  <behavior>
    - Test 1: 非 macOS 构建返回全 false marker，普通内容仍可 `Persist`。
    - Test 2: monitor 在命中 `Skip` 或 `CurrentOnly` 时不会把条目送进 runtime save loop。
    - Test 3: marker 采集发生在 `ContentDetector::detect(...)` 之前。
  </behavior>
  <action>新增 `src-tauri/src/capture/macos_markers.rs`，在 macOS 下通过已有 `cocoa`/`objc` 读取这些明确 marker：`org.nspasteboard.TransientType`、`org.nspasteboard.ConcealedType`、`org.nspasteboard.AutoGeneratedType`、`org.nspasteboard.source`、`com.apple.is-remote-clipboard`。非 macOS 下返回 capability-based no-op：所有布尔字段为 `false`、`source_bundle_id = None`。把 `src-tauri/src/clipboard/monitor.rs` 改成先读取 `PasteboardMarkers`，再结合排除应用与文本大小校验调用 `decide_capture(...)`。只有 `CaptureDisposition::Persist` 才允许进入 `ContentDetector::detect(...)` 与后续 `tx.send(entry)`；`CurrentOnly` 在 Phase 1 中只更新去重所需的内存态，不允许落库。不要把 hash-based self-write suppression重复实现到 monitor；继续复用 Plan 02 的 runtime 抑制机制。</action>
  <acceptance_criteria>
    - `rg -n "org\\.nspasteboard\\.TransientType|org\\.nspasteboard\\.ConcealedType|org\\.nspasteboard\\.AutoGeneratedType|com\\.apple\\.is-remote-clipboard|org\\.nspasteboard\\.source" src-tauri/src/capture/macos_markers.rs` 有匹配结果。
    - `rg -n "decide_capture|CaptureDisposition::Persist|CaptureDisposition::CurrentOnly|CaptureDisposition::Skip" src-tauri/src/clipboard/monitor.rs` 有匹配结果。
    - `rg -n "ContentDetector::detect" src-tauri/src/clipboard/monitor.rs` 仍存在，但其前面已经有 marker/policy 判定逻辑。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_capture_policy_current_only_is_non_persistent_in_v1 -- --nocapture</automated>
  </verify>
  <done>监控链路已经在 subtype 检测和数据库写入之前做 marker-first 策略过滤，CAPT-03 具备明确平台边界和自动化信号。</done>
</task>

</tasks>

<verification>
先运行 `cd src-tauri && cargo test test_capture_policy_marker_matrix -- --nocapture`，再运行 `cd src-tauri && cargo test test_capture_policy_current_only_is_non_persistent_in_v1 -- --nocapture`。自动化通过后，再在 macOS 真机做一次手动 smoke：触发普通复制、应用内回贴、自带 remote/transient/concealed 特征的复制流程，确认只有正常事件进入历史。
</verification>

<success_criteria>
CAPT-03 的“哪些事件不该持久化”已被 marker-first policy 明确定义并接入 monitor，非 macOS 保持 no-op 能力边界而不破坏现有采集。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-04-SUMMARY.md`
</output>
