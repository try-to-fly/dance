---
phase: 04-search-quality-retrieval
plan: 01
type: execute
wave: 1
depends_on:
  - 03-01
  - 03-02
  - 03-05
files_modified:
  - src-tauri/src/retrieval/mod.rs
  - src-tauri/src/retrieval_tests.rs
  - src-tauri/src/state.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src/stores/clipboardStore.ts
  - src/types/clipboard.ts
  - src/components/SearchBar/SearchBar.tsx
  - src/components/ClipboardList/RetrievalFilterBar.tsx
  - src/components/ClipboardList/ClipboardList.tsx
  - src/components/ClipboardList/ClipboardItem.tsx
  - src/components/ClipboardList/EmptyState.tsx
autonomous: true
requirements:
  - RETR-01
  - RETR-02
  - RETR-03
  - RETR-04
  - RETR-05
must_haves:
  truths:
    - '检索权威层收口到 Rust + SQLite：前端不再对结果集做文本 includes 或类型二次裁剪，所有 query/filter 都走 `search_clipboard_history`。'
    - '统一 query model 支持文本、类型或 subtype、来源应用、收藏和时间窗口，并能通过 `list_clipboard_source_apps` 提供来源应用候选。'
    - '结构化 token 与模糊召回覆盖 URL host/query、JSON key path、命令或代码 token、颜色值等开发者高频场景，并且 `api.example`、`deploy.service` 这类带点 token 不再触发 FTS 语法错误。'
    - '检索结果继续复用 Phase 03 的 `buildPreviewSummary(entry, ''retrieval'')`，并额外带上 `snippet` 与 `match_kind`，让用户能看懂为什么命中。'
    - '空历史和无结果状态在 UI 上显式区分，避免 retrieval state 下把“暂无记录”和“筛选后没有结果”混成同一种空白。'
  artifacts:
    - path: src-tauri/src/retrieval/mod.rs
      provides: 'authoritative retrieval query、ranking、snippet 与 search document persistence'
    - path: src-tauri/src/retrieval_tests.rs
      provides: '结构化 token、模糊匹配、组合过滤和来源应用列表回归测试'
    - path: src/stores/clipboardStore.ts
      provides: 'thin-client retrieval query adapter 与统一 fetch/load-more/filter orchestration'
    - path: src/components/ClipboardList/RetrievalFilterBar.tsx
      provides: '来源应用、收藏、时间窗口过滤 UI'
    - path: src/components/ClipboardList/ClipboardItem.tsx
      provides: 'retrieval density 下的 snippet 与命中原因展示'
  key_links:
    - from: src/components/SearchBar/SearchBar.tsx
      to: src/stores/clipboardStore.ts
      via: debounced query + retrieval filter state
      pattern: setSearchTerm
    - from: src/stores/clipboardStore.ts
      to: src-tauri/src/commands.rs
      via: `search_clipboard_history` / `list_clipboard_source_apps`
      pattern: invoke
    - from: src-tauri/src/commands.rs
      to: src-tauri/src/retrieval/mod.rs
      via: AppState authoritative retrieval path
      pattern: search_clipboard_history
    - from: src/types/clipboard.ts
      to: src/components/ClipboardList/ClipboardItem.tsx
      via: `ClipboardRetrievalMatch`
      pattern: retrieval
---

<objective>
交付 Phase 04 的 authoritative local retrieval 链路，让搜索、筛选、排序与命中解释都统一落在 Rust + SQLite 上，并把前端收缩成 query 输入和结果渲染层。

Purpose: 这是 RETR-01..05 的闭环。没有这一步，列表仍会停留在 backend 搜索 + frontend 二次 filter 的 split-brain 状态，随着历史增大无法保持一致和可解释。
Output: `search_clipboard_history`、`list_clipboard_source_apps`、retrieval match/snippet contract，以及 retrieval-aware list/filter UI。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-search-quality-retrieval/04-CONTEXT.md
@.planning/phases/04-search-quality-retrieval/04-RESEARCH.md
@.planning/phases/04-search-quality-retrieval/04-UI-SPEC.md
@src-tauri/src/retrieval/mod.rs
@src/stores/clipboardStore.ts
@src/components/SearchBar/SearchBar.tsx
@src/components/ClipboardList/ClipboardList.tsx

<interfaces>
Phase 04 收口后的核心 contract：

```ts
export interface ClipboardHistoryQuery {
  text?: string;
  selected_type?: string;
  source_app?: string;
  favorites_only?: boolean;
  recency_days?: number;
  limit?: number;
  offset?: number;
}

export interface ClipboardRetrievalMatch {
  match_kind: string;
  label: string;
  snippet?: string | null;
}
```

```rust
pub async fn search_clipboard_history(
    pool: &Pool<Sqlite>,
    query: ClipboardHistoryQuery,
) -> Result<Vec<ClipboardEntry>>;

pub async fn list_clipboard_source_apps(
    &self,
    limit: Option<i32>,
) -> Result<Vec<String>>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 完成 Rust authoritative retrieval query、search documents 与结构化检索测试</name>
  <files>src-tauri/src/retrieval/mod.rs, src-tauri/src/retrieval_tests.rs, src-tauri/src/state.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs</files>
  <read_first>.planning/phases/04-search-quality-retrieval/04-CONTEXT.md, .planning/phases/04-search-quality-retrieval/04-RESEARCH.md, src-tauri/src/retrieval/mod.rs, src-tauri/src/state.rs, src-tauri/src/commands.rs</read_first>
  <behavior>
    - Test 1: 搜索 `api.example`、`deploy.service`、颜色值和缩写片段时，能命中 URL host、JSON key 和 color structured tokens，且不出现 FTS 语法错误。
    - Test 2: `selected_type`、`source_app`、`favorites_only`、`recency_days` 能组合生效，后端返回结果就是最终事实，不再依赖前端再裁一次。
    - Test 3: 来源应用列表能按最近活跃顺序返回去重结果，供 UI filter 直接消费。
  </behavior>
  <action>在 `src-tauri/src/retrieval/mod.rs` 中完成 retrieval companion document 的 upsert、FTS 查询、稳定排序、`snippet`/`match_kind` 映射和点号 token 转义；在 `state.rs`、`commands.rs`、`lib.rs` 暴露统一 retrieval path 与来源应用查询；在 `src-tauri/src/retrieval_tests.rs` 为 URL host、JSON key path、颜色值、来源应用列表和组合过滤补齐回归测试。</action>
  <acceptance_criteria>
    - `rg -n "pub async fn search_clipboard_history|ClipboardHistoryQuery|ClipboardRetrievalMatch" src-tauri/src/retrieval/mod.rs src-tauri/src/state.rs src-tauri/src/commands.rs` 有匹配结果。
    - `rg -n "api.example|deploy.service|favorites_only|recency_days|list_clipboard_source_apps" src-tauri/src/retrieval_tests.rs` 有匹配结果。
    - `rg -n "escape|MATCH|bm25|snippet" src-tauri/src/retrieval/mod.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test</automated>
  </verify>
  <done>Rust authoritative retrieval 已可承载结构化 token、模糊片段和组合 filter，并有后端回归测试兜底。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把前端收口为 retrieval thin client，并渲染命中原因、snippet 与 retrieval 空状态</name>
  <files>src/stores/clipboardStore.ts, src/types/clipboard.ts, src/components/SearchBar/SearchBar.tsx, src/components/ClipboardList/RetrievalFilterBar.tsx, src/components/ClipboardList/ClipboardList.tsx, src/components/ClipboardList/ClipboardItem.tsx, src/components/ClipboardList/EmptyState.tsx</files>
  <read_first>.planning/phases/04-search-quality-retrieval/04-UI-SPEC.md, src/stores/clipboardStore.ts, src/components/SearchBar/SearchBar.tsx, src/components/ClipboardList/ClipboardItem.tsx, src/components/ClipboardList/ClipboardList.tsx</read_first>
  <behavior>
    - Test 1: `clipboardStore` 只把 query/filter 状态转成 backend contract，不再做本地 includes/filter。
    - Test 2: retrieval state 下，列表卡片使用 `buildPreviewSummary(entry, 'retrieval')`，并展示 `snippet`、`match reason`、来源应用/收藏/时间筛选结果。
    - Test 3: 空历史与 no-results 各有独立 empty state，搜索过程中保留旧结果并显示 retrieval loading copy。
  </behavior>
  <action>在 `clipboardStore.ts` 中新增统一 query adapter、来源应用选项加载和 retrieval active 判定，让 `getFilteredEntries()` 退化为直接返回当前 entries；在 `SearchBar.tsx` 和新增的 `RetrievalFilterBar.tsx` 中接入来源应用、收藏、时间窗口 filter；在 `ClipboardItem.tsx`、`ClipboardList.tsx` 和 `EmptyState.tsx` 中落地 retrieval density、snippet、命中原因、空状态与加载文案。</action>
  <acceptance_criteria>
    - `rg -n "getFilteredEntries: \\(\\) => get\\(\\)\\.entries|isRetrievalActive|list_clipboard_source_apps" src/stores/clipboardStore.ts` 有匹配结果。
    - `rg -n "selectedSourceApp|favoritesOnly|recencyDays" src/components/SearchBar/SearchBar.tsx src/components/ClipboardList/RetrievalFilterBar.tsx src/components/ClipboardList/ClipboardList.tsx` 有匹配结果。
    - `rg -n "snippet|match_kind|noResultsTitle|clearFilters" src/components/ClipboardList/ClipboardItem.tsx src/components/ClipboardList/EmptyState.tsx` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test && pnpm build</automated>
  </verify>
  <done>前端现在只负责 retrieval query 输入和结果展示，检索权威不再分裂，命中原因和空状态也能被用户直接理解。</done>
</task>

</tasks>

<verification>
运行 `cargo test` 验证 retrieval backend contract 与回归样本；运行 `pnpm test` 和 `pnpm build` 验证前端 store/UI contract、类型与构建链路。
</verification>

<success_criteria>
Phase 04 完成后，用户能用同一条本地检索链路快速搜索和筛选大历史，并在结果列表里直接理解“为什么命中”和“当前没有结果”的语义。
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-quality-retrieval/04-01-SUMMARY.md`
</output>
