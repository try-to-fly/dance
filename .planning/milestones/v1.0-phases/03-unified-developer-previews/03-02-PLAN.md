---
phase: 03-unified-developer-previews
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/ClipboardList/ClipboardItem.tsx
  - src/components/ClipboardList/ClipboardItem.test.tsx
  - src/components/ClipboardList/ClipboardList.tsx
autonomous: true
requirements:
  - PREV-05
must_haves:
  truths:
    - '列表项通过统一的两层摘要展示 `headline + secondarySummary`，而不是每个 subtype 都渲染一套独立预览卡，落实 D-05、D-06。'
    - '列表项保持固定紧凑高度和固定 virtualizer estimate，不会因为图片、JSON、颜色或长文本撑高单条卡片，落实 D-07。'
    - '列表不再依赖 `getImageUrl()`、resolved preview 或远端 URL enrichment；同一条内容在 list 和 detail 至少共享相同 `semanticType` 与 `previewIntent`。'
  artifacts:
    - path: src/components/ClipboardList/ClipboardItem.tsx
      provides: '统一两层 list summary shell'
    - path: src/components/ClipboardList/ClipboardItem.test.tsx
      provides: '固定高度、双层摘要、长内容截断的组件回归测试'
    - path: src/components/ClipboardList/ClipboardList.tsx
      provides: '与固定 item 高度一致的 virtualizer estimate'
  key_links:
    - from: src/components/ClipboardList/ClipboardItem.tsx
      to: src/lib/preview/previewSummary.ts
      via: buildPreviewSummary(entry, 'list')
      pattern: buildPreviewSummary
    - from: src/components/ClipboardList/ClipboardList.tsx
      to: src/components/ClipboardList/ClipboardItem.tsx
      via: fixed item estimate
      pattern: estimateSize
---

<objective>
把剪贴板列表从 subtype-specific 卡片集合收口成统一的高密度语义摘要列表，保证它和 detail / retrieval 使用同一份 semantic preview contract。

Purpose: 这是 Phase 03 对 D-05..D-08 的直接实现，也是 PREV-05 在 list 表面的落地点。列表必须快、稳、密，不应该继续依赖 detail 式 preview 分支。
Output: 使用 `buildPreviewSummary(entry, 'list')` 的 `ClipboardItem`、固定高度 list virtualization 和组件测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-unified-developer-previews/03-CONTEXT.md
@.planning/phases/03-unified-developer-previews/03-RESEARCH.md
@src/components/ClipboardList/ClipboardItem.tsx
@src/components/ClipboardList/ClipboardList.tsx
@src/lib/preview/previewSummary.ts

<interfaces>
本计划必须直接消费 03-01 输出的 contract：

```ts
export interface PreviewSummaryDescriptor {
  density: 'list' | 'retrieval';
  semanticType: string;
  previewIntent: string;
  headline: string;
  secondarySummary: string;
}

export function buildPreviewSummary(
  entry: ClipboardEntry,
  density: 'list' | 'retrieval'
): PreviewSummaryDescriptor;
```

当前 brownfield 问题入口：

```tsx
const renderPreview = () => {
  if (isImageEntry) { ... }
  if (entry.content_subtype === 'color') { ... }
  if (entry.content_subtype === 'timestamp') { ... }
  return <div>...</div>;
};
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为统一两层 list summary 建立组件回归测试</name>
  <files>src/components/ClipboardList/ClipboardItem.test.tsx</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, src/components/ClipboardList/ClipboardItem.tsx, src/components/ClipboardList/ClipboardList.tsx, src/lib/preview/previewSummary.ts</read_first>
  <behavior>
    - Test 1: `ClipboardItem` 对 JSON、URL、颜色、代码、命令至少五类条目渲染同一套两层结构，headline 在第一层，secondary summary 在第二层，落实 D-06、D-08。
    - Test 2: 卡片高度固定，headline 单行截断、secondary summary 双行截断，长内容不会把卡片拉高，落实 D-07。
    - Test 3: list item 不再请求图片预览或 resolved preview，证明 summary contract 是同步的、analysis-first 的。
  </behavior>
  <action>新增 `src/components/ClipboardList/ClipboardItem.test.tsx`。直接用真实 `buildPreviewSummary()` 驱动断言，不要 mock 掉 summary contract。至少覆盖 JSON、URL、颜色、代码、命令五类的 headline / secondary summary；对组件根节点断言固定高度 class，例如 `h-[118px]` 或等价固定高度 class；对 headline 和 secondary summary 断言 `line-clamp-1` / `line-clamp-2`。把 `useClipboardStore()` 里的 `getImageUrl` 设成 mock，并断言整个渲染过程不会调用它。</action>
  <acceptance_criteria>
    - `rg -n "JSON|URL|Color|Code|Command|line-clamp-1|line-clamp-2" src/components/ClipboardList/ClipboardItem.test.tsx` 有匹配结果。
    - `rg -n "getImageUrl" src/components/ClipboardList/ClipboardItem.test.tsx` 有匹配结果，且测试断言其未被调用。
    - `rg -n "buildPreviewSummary" src/components/ClipboardList/ClipboardItem.test.tsx` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/components/ClipboardList/ClipboardItem.test.tsx</automated>
  </verify>
  <done>列表两层摘要 contract 已被组件测试锁定，执行者可以无歧义地把 `ClipboardItem` 从 subtype 分支卡片改成统一摘要壳。</done>
</task>

<task type="auto">
  <name>Task 2: 把 `ClipboardItem` 收口到固定高度摘要壳，并同步 virtualizer estimate</name>
  <files>src/components/ClipboardList/ClipboardItem.tsx, src/components/ClipboardList/ClipboardList.tsx, src/components/ClipboardList/ClipboardItem.test.tsx</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, src/components/ClipboardList/ClipboardItem.tsx, src/components/ClipboardList/ClipboardList.tsx, src/lib/preview/previewSummary.ts, src/components/ClipboardList/ClipboardItem.test.tsx</read_first>
  <action>重写 `ClipboardItem.tsx` 的 preview 主体，统一改为 `const summary = buildPreviewSummary(entry, 'list')` 驱动的两层结构：第一层只显示 `summary.headline`，第二层只显示 `summary.secondarySummary`，不要继续保留 `renderPreview()` 里的 image/color/timestamp/text 分支卡片。去掉 `getImageUrl()`、`imageUrl` state 和与 resolved preview 相关的副作用；保留 source app、time、favorite、copy count 等外围 meta。卡片高度固定为一个明确值，例如 `h-[118px] min-[1200px]:h-[126px]`，并把 `ClipboardList.tsx` 的 `estimateSize` 同步更新到与该固定高度一致的值，例如 `124`，不要再让 virtualizer 估测旧卡片高度。这个任务只改 list 表面，不要把 retrieval 布局提前做出来。</action>
  <acceptance_criteria>
    - `rg -n "buildPreviewSummary\\(entry, 'list'\\)" src/components/ClipboardList/ClipboardItem.tsx` 有匹配结果。
    - `rg -n "getImageUrl|imageUrl|renderPreview" src/components/ClipboardList/ClipboardItem.tsx` 无匹配结果。
    - `rg -n "h-\\[118px\\]|h-\\[126px\\]|line-clamp-1|line-clamp-2" src/components/ClipboardList/ClipboardItem.tsx` 有匹配结果。
    - `rg -n "estimateSize: \\(\\) => 124" src/components/ClipboardList/ClipboardList.tsx` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/components/ClipboardList/ClipboardItem.test.tsx</automated>
  </verify>
  <done>列表已从 subtype-specific preview 卡片收口为固定高度语义摘要列表，并与共享 summary contract 对齐。</done>
</task>

</tasks>

<verification>
运行 `ClipboardItem` 组件测试，确认五类高频类型都走同一套摘要壳；再人工快速检查 `ClipboardList.tsx` 的 virtualizer estimate 与固定 item 高度一致。
</verification>

<success_criteria>
列表滚动和密度不被单条内容破坏，且同一条内容在 list 与 detail 至少共享同一 semantic type / preview intent。
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-developer-previews/03-02-SUMMARY.md`
</output>
