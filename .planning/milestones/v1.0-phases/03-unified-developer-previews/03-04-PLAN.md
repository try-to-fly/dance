---
phase: 03-unified-developer-previews
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/DetailView/ContentRenderers/ColorRenderer.tsx
  - src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx
autonomous: true
requirements:
  - PREV-03
  - PREV-05
must_haves:
  truths:
    - '颜色条目 detail 默认展示可见 swatch 和确定顺序的 HEX / RGB / RGBA / HSL 格式，符合 PREV-03。'
    - '颜色 renderer 优先消费 analysis-first metadata，缺失 metadata 时才本地解析 raw content，不再依赖调试日志或不稳定格式顺序。'
    - '每个可见颜色格式都能通过统一 backend copy 合同复制，不出现前端直写系统剪贴板的分支。'
  artifacts:
    - path: src/components/DetailView/ContentRenderers/ColorRenderer.tsx
      provides: '稳定的颜色 swatch + format grid renderer'
    - path: src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx
      provides: '颜色 detail 的 swatch / format / copy contract tests'
  key_links:
    - from: src/components/DetailView/ContentRenderers/ColorRenderer.tsx
      to: src/stores/clipboardStore.ts
      via: useClipboardStore().copyToClipboard
      pattern: copyToClipboard
---

<objective>
把颜色 detail renderer 打磨成稳定、开发友好的 swatch + format grid，而不是继续保留调试日志和半临时格式推断。

Purpose: PREV-03 的价值不在“能识别颜色”，而在 detail 里直接给开发者一个可靠的颜色工作面板。这个能力应独立于 URL、本地 workbench 和搜索功能推进。
Output: 清理后的 `ColorRenderer` 与对应组件测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-developer-previews/03-CONTEXT.md
@.planning/phases/03-unified-developer-previews/03-RESEARCH.md
@src/components/DetailView/ContentRenderers/ColorRenderer.tsx
@src/stores/clipboardStore.ts

<interfaces>
当前 Color renderer 入口：

```ts
interface ColorRendererProps {
  content: string;
  metadata?: string | null;
}
```

复制合同必须继续复用：

```ts
const { copyToClipboard } = useClipboardStore();
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为颜色 swatch / formats / copy contract 建立组件测试</name>
  <files>src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, src/components/DetailView/ContentRenderers/ColorRenderer.tsx, src/stores/clipboardStore.ts</read_first>
  <behavior>
    - Test 1: renderer 显示色块 swatch 和固定顺序的 HEX / RGB / RGBA / HSL 四组格式，落实 PREV-03。
    - Test 2: 优先使用 metadata 里的 `color_formats`，只有 metadata 缺失时才回退本地解析。
    - Test 3: 原始值和各格式 copy 都走 backend copy contract，不使用 `navigator.clipboard` 或其他前端直写路径。
  </behavior>
  <action>新增 `src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx`。使用 metadata 存在和 metadata 缺失两个场景，分别断言 swatch、格式顺序和按钮 copy 行为。测试必须 mock `useClipboardStore().copyToClipboard` 并验证点击“复制原始值”与各格式 copy icon 时都走同一个 store contract。不要把 `console.log` 之类的调试输出写进测试预期。</action>
  <acceptance_criteria>
    - `rg -n "HEX|RGB|RGBA|HSL|copyToClipboard" src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx` 有匹配结果。
    - `rg -n "metadata" src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx` 有匹配结果，证明覆盖 metadata 优先路径。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx</automated>
  </verify>
  <done>颜色 detail 的主要可见行为已被测试锁定，执行者可以安全地清理现有调试逻辑并稳定格式输出。</done>
</task>

<task type="auto">
  <name>Task 2: 清理 `ColorRenderer` 并收口成稳定的开发友好格式面板</name>
  <files>src/components/DetailView/ContentRenderers/ColorRenderer.tsx, src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx</files>
  <read_first>.planning/phases/03-unified-developer-previews/03-CONTEXT.md, src/components/DetailView/ContentRenderers/ColorRenderer.tsx, src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx</read_first>
  <action>重构 `ColorRenderer.tsx`：优先读取 `metadata.color_formats`，仅在其缺失时使用 `color-convert` 本地推导；确保格式输出顺序固定为 HEX、RGB、RGBA、HSL；保留主 swatch、RGB 数值和“复制原始值”按钮，但删除现有 `console.log` / `console.error('解析颜色元数据失败')` 这类调试噪音。copy 入口全部继续使用 `useClipboardStore().copyToClipboard`。不要在这个任务里改动 `PrimaryPreviewRenderer.tsx` 或引入新的颜色检测逻辑，Phase 03 只统一 preview，不扩展 detection。</action>
  <acceptance_criteria>
    - `rg -n "console\\.log" src/components/DetailView/ContentRenderers/ColorRenderer.tsx` 无匹配结果。
    - `rg -n "HEX:|RGB:|RGBA:|HSL:" src/components/DetailView/ContentRenderers/ColorRenderer.tsx` 有匹配结果。
    - `rg -n "copyToClipboard" src/components/DetailView/ContentRenderers/ColorRenderer.tsx` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/components/DetailView/ContentRenderers/ColorRenderer.test.tsx</automated>
  </verify>
  <done>颜色 detail renderer 已稳定展示 swatch 与多格式结果，并统一走 backend copy 合同。</done>
</task>

</tasks>

<verification>
运行 `ColorRenderer` 组件测试，确认 metadata 优先、fallback 解析和所有 copy 入口都稳定工作。
</verification>

<success_criteria>
颜色条目 detail 现在是一个稳定可用的开发者颜色面板，而不是带调试输出的半成品 renderer。
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-developer-previews/03-04-SUMMARY.md`
</output>
