---
phase: 01-capture-reliability-storage-cohesion
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src-tauri/src/app_paths.rs
  - src-tauri/src/database/mod.rs
  - src-tauri/src/config/mod.rs
  - src-tauri/src/clipboard/processor.rs
  - src-tauri/src/utils/app_icon_extractor.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/state.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/app_paths_tests.rs
autonomous: true
requirements:
  - CAPT-04
must_haves:
  truths:
    - '配置、数据库、图片资源、图标缓存和日志都由一个权威路径层解析。'
    - '旧的 `dance` 与 `clipboard-app` 根目录迁移是显式且幂等的。'
    - '路径与迁移验证可以完全在 temp-root 中跑通。'
  artifacts:
    - path: 'src-tauri/src/app_paths.rs'
      provides: 'AppPaths 路径权威与迁移逻辑'
    - path: 'src-tauri/src/app_paths_tests.rs'
      provides: 'CAPT-04 自动化验证'
  key_links:
    - from: 'src-tauri/src/app_paths.rs'
      to: 'src-tauri/src/database/mod.rs'
      via: 'history_db_path()'
      pattern: 'history_db_path'
    - from: 'src-tauri/src/app_paths.rs'
      to: 'src-tauri/src/clipboard/processor.rs'
      via: 'image_assets_dir()'
      pattern: 'image_assets_dir'
    - from: 'src-tauri/src/app_paths.rs'
      to: 'src-tauri/src/commands.rs'
      via: 'resolve_relative_asset_path()'
      pattern: 'resolve_relative_asset_path'
---

<objective>
建立 `AppPaths` 作为唯一存储权威，并完成从 `dance` / `clipboard-app` 的显式迁移。

Purpose: 先让“应用的数据到底在哪”这件事变成单一答案，CAPT-04 才能被验证和维护。
Output: `AppPaths` 模块、注入式路径构造、遗留根目录迁移、以及对应 CAPT-04 自动化测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md
@.planning/phases/01-capture-reliability-storage-cohesion/01-VALIDATION.md
@src-tauri/src/test_support.rs
@src-tauri/src/database/mod.rs
@src-tauri/src/config/mod.rs
@src-tauri/src/clipboard/processor.rs
@src-tauri/src/utils/app_icon_extractor.rs
@src-tauri/src/commands.rs
@src-tauri/src/state.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 AppPaths 权威层并把核心存储构造改成注入式</name>
  <files>src-tauri/src/app_paths.rs, src-tauri/src/database/mod.rs, src-tauri/src/config/mod.rs, src-tauri/src/clipboard/processor.rs, src-tauri/src/state.rs, src-tauri/src/lib.rs, src-tauri/src/app_paths_tests.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src-tauri/src/test_support.rs, src-tauri/src/database/mod.rs, src-tauri/src/config/mod.rs, src-tauri/src/clipboard/processor.rs, src-tauri/src/state.rs, src-tauri/src/lib.rs, src-tauri/src/app_paths_tests.rs</read_first>
  <behavior>
    - Test 1: `AppPaths` 在 temp-root 下生成固定布局：`config/config.json`、`data/clipboard.db`、`data/imgs/`、`cache/icons/`、`logs/`。
    - Test 2: `Database`、`ConfigManager`、`ContentProcessor` 不再自己调用 `dirs::config_dir()`，而是接收注入的 `Arc<AppPaths>`。
    - Test 3: `test_app_paths_injected_roots_drive_core_modules` 在 temp-root 下实例化 `Database::new_in`、`ConfigManager::new_in`、`ContentProcessor::new_in`，并断言它们解析出的路径全部落在注入的 `AppPaths` 根目录下。
  </behavior>
  <action>新增 `src-tauri/src/app_paths.rs`，定义 `pub struct AppPaths`，并提供这些明确方法：`from_app(app: &AppHandle) -> Result<Self>`、`from_roots(config_dir: PathBuf, data_dir: PathBuf, cache_dir: PathBuf, log_dir: PathBuf) -> Self`、`config_file_path() -> PathBuf`、`history_db_path() -> PathBuf`、`image_assets_dir() -> PathBuf`、`icon_cache_dir() -> PathBuf`、`log_dir() -> PathBuf`、`migration_marker_path() -> PathBuf`、`resolve_relative_asset_path(relative: &str) -> Result<PathBuf>`。路径映射固定为：配置=`app_config_dir()/config.json`，数据库=`app_data_dir()/clipboard.db`，持久图片=`app_data_dir()/imgs`，图标缓存=`app_cache_dir()/icons`，日志=`app_log_dir()`。把 `Database::new()`、`ConfigManager::new()`、`ContentProcessor::new()` 改成委托给 `new_in(paths: Arc<AppPaths>)`。`AppState::new()` 必须接收 `Arc<AppPaths>`，`src-tauri/src/lib.rs` 在创建 `AppState` 前先构造这套路径对象并传入，禁止继续在运行时构造阶段散落字符串路径。同步在 `src-tauri/src/app_paths_tests.rs` 新增 `test_app_paths_injected_roots_drive_core_modules`：用 Plan 01 的 temp-root helper 构造 `Arc<AppPaths>`，实例化 `Database::new_in(paths.clone())`、`ConfigManager::new_in(paths.clone())`、`ContentProcessor::new_in(paths.clone())`，并断言配置文件、数据库文件、图片目录都来自注入的 `AppPaths` 根，而不是任何真实用户目录。</action>
  <acceptance_criteria>
    - `rg -n "pub struct AppPaths|from_app|from_roots|config_file_path|history_db_path|image_assets_dir|icon_cache_dir|migration_marker_path|resolve_relative_asset_path" src-tauri/src/app_paths.rs` 有匹配结果。
    - `rg -n "new_in\\(" src-tauri/src/database/mod.rs src-tauri/src/config/mod.rs src-tauri/src/clipboard/processor.rs` 每个文件至少有 1 个匹配结果。
    - `rg -n "Arc<AppPaths>|AppPaths::from_app" src-tauri/src/state.rs src-tauri/src/lib.rs` 有匹配结果。
    - `rg -n "test_app_paths_injected_roots_drive_core_modules|Database::new_in|ConfigManager::new_in|ContentProcessor::new_in" src-tauri/src/app_paths_tests.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_app_paths_injected_roots_drive_core_modules -- --nocapture</automated>
  </verify>
  <done>`AppPaths` 已成为权威接口，后续代码不需要再各自推导自己的存储根目录。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 legacy migration 并替换残留的路径字符串拼接</name>
  <files>src-tauri/src/app_paths.rs, src-tauri/src/utils/app_icon_extractor.rs, src-tauri/src/commands.rs, src-tauri/src/state.rs, src-tauri/src/app_paths_tests.rs, src-tauri/src/lib.rs</files>
  <read_first>.planning/phases/01-capture-reliability-storage-cohesion/01-RESEARCH.md, src-tauri/src/app_paths.rs, src-tauri/src/utils/app_icon_extractor.rs, src-tauri/src/commands.rs, src-tauri/src/state.rs, src-tauri/src/app_paths_tests.rs</read_first>
  <behavior>
    - Test 1: 当 temp-root 中同时存在 `dance/clipboard.db`、`dance/config.json`、`clipboard-app/imgs/*`、`clipboard-app/icons/*` 时，迁移后新根目录可读取全部资产。
    - Test 2: 当目标文件已存在时，迁移保持新根目录优先，并写出幂等标记。
  </behavior>
  <action>在 `AppPaths` 中实现 `migrate_legacy_roots()`，遗留输入只认 `dirs::config_dir()/dance` 和 `dirs::config_dir()/clipboard-app` 这两个根。迁移规则固定为：`dance/config.json -> config/config.json`，`dance/clipboard.db -> data/clipboard.db`，`clipboard-app/imgs/* -> data/imgs/*`，`clipboard-app/icons/* -> cache/icons/*`。迁移完成后写 `data/migrations/capt04-storage-roots.json`，内容至少包含 `version: "capt04-storage-roots"`、`migrated_from`、`completed_at`。把 `AppIconExtractor`、`open_file_with_system`、`get_image_url`、`AppState::get_cache_statistics`、图片路径解析和数据库路径解析全部改成走 `AppPaths`。在 `src-tauri/src/app_paths_tests.rs` 去掉 `test_app_paths_migrate_legacy_roots` 上的 ignore，并实现真实断言。`src-tauri/src/lib.rs` 在创建 `AppState` 前必须显式调用迁移逻辑。</action>
  <acceptance_criteria>
    - `rg -n "migrate_legacy_roots|capt04-storage-roots|completed_at" src-tauri/src/app_paths.rs src-tauri/src/app_paths_tests.rs` 有匹配结果。
    - `rg -n "dirs::config_dir\\(" src-tauri/src/database/mod.rs src-tauri/src/config/mod.rs src-tauri/src/clipboard/processor.rs src-tauri/src/utils/app_icon_extractor.rs src-tauri/src/commands.rs src-tauri/src/state.rs` 无匹配结果。
    - `rg -n "#\\[ignore = \"implemented in 01-02\"\\]" src-tauri/src/app_paths_tests.rs` 无匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_app_paths_migrate_legacy_roots -- --nocapture</automated>
  </verify>
  <done>CAPT-04 所需的权威路径与遗留迁移已经落地，路径相关自动化测试可在隔离环境中验证真实行为。</done>
</task>

</tasks>

<verification>
先执行 `cd src-tauri && cargo test test_app_paths_injected_roots_drive_core_modules -- --nocapture`，确认核心模块真的消费了注入式 `AppPaths`。该命令通过后，再执行 `cd src-tauri && cargo test test_app_paths_migrate_legacy_roots -- --nocapture`，最后补跑 `cd src-tauri && cargo test --no-run` 检查没有残留路径调用破坏编译。
</verification>

<success_criteria>
应用的本地存储生命周期已经由 `AppPaths` 统一定义，旧根目录迁移显式且幂等，CAPT-04 有稳定自动化信号。
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-reliability-storage-cohesion/01-02-SUMMARY.md`
</output>
