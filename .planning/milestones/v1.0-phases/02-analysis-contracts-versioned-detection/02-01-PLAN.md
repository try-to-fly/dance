---
phase: 02-analysis-contracts-versioned-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/analysis/mod.rs
  - src-tauri/src/analysis/contract.rs
  - src-tauri/src/analysis/service.rs
  - src-tauri/src/analysis_contract_tests.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - DETE-01
  - DETE-02
  - DETE-04
must_haves:
  truths:
    - 'Rust 侧存在单一的 `AnalysisSnapshot` 权威合同，后续 capture、入库、读取和前端消费都围绕它，而不是继续散落在 tuple 和字符串字段里。'
    - '支持的 developer subtype 优先级、contract version 和 analysis version 有固定测试，不再只依赖 `ContentDetector::detect()` 的隐式顺序。'
    - '合同层可以显式表示 `matched` / `fallback`、typed metadata 和 diagnostics，即使本计划尚未接到实际持久化链路。'
  artifacts:
    - path: 'src-tauri/src/analysis/contract.rs'
      provides: 'Phase 2 的 AnalysisSnapshot、AnalysisStatus、AnalysisSubtype、typed metadata 与 diagnostics 合同'
    - path: 'src-tauri/src/analysis/service.rs'
      provides: 'Rust authoritative text analysis service API 与统一入口'
    - path: 'src-tauri/src/analysis_contract_tests.rs'
      provides: 'subtype corpus、precedence、fallback serialization contract tests'
  key_links:
    - from: 'src-tauri/src/analysis/service.rs'
      to: 'src-tauri/src/clipboard/content_detector.rs'
      via: 'contract adapter over existing detector heuristics'
      pattern: 'ContentDetector::detect'
    - from: 'src-tauri/src/lib.rs'
      to: 'src-tauri/src/analysis_contract_tests.rs'
      via: 'cfg(test) module registration'
      pattern: 'mod analysis_contract_tests'
---

<objective>
建立 Phase 2 的 Rust 权威 analysis contract：稳定 subtype、typed metadata、fallback status、diagnostics、version 常量和统一 service seam 一次性固定下来。

Purpose: 先把语义边界立住，后续 companion table、capture integration、reanalysis 和前端消费才不会继续围绕 `clipboard_entries.content_subtype` / `metadata` 的旧字符串接口打补丁。
Output: `analysis` 模块骨架、`AnalysisSnapshot` 合同、`TextAnalysisService` 入口，以及可执行的 contract tests。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md
@src-tauri/src/clipboard/content_detector.rs
@src-tauri/src/models/mod.rs
@src-tauri/src/lib.rs

<interfaces>
当前 detector 的公开形状：

```rust
pub enum ContentSubType {
    PlainText,
    Url,
    IpAddress,
    Email,
    Color,
    Code,
    Command,
    Timestamp,
    Json,
    Markdown,
    Base64,
}

pub struct ContentMetadata {
    pub detected_language: Option<String>,
    pub url_parts: Option<UrlParts>,
    pub color_formats: Option<ColorFormats>,
    pub timestamp_formats: Option<TimestampFormats>,
    pub base64_metadata: Option<Base64Metadata>,
}

impl ContentDetector {
    pub fn detect(text: &str) -> (ContentSubType, Option<ContentMetadata>);
}
```

当前 Phase 2 不能继续扩散的 brownfield 事实：

```rust
pub struct ClipboardEntry {
    pub content_subtype: Option<String>,
    pub metadata: Option<String>,
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 `analysis` 模块与 Rust authoritative `AnalysisSnapshot` 合同</name>
  <files>src-tauri/src/analysis/mod.rs, src-tauri/src/analysis/contract.rs, src-tauri/src/analysis/service.rs, src-tauri/src/lib.rs</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md, src-tauri/src/clipboard/content_detector.rs, src-tauri/src/models/mod.rs, src-tauri/src/lib.rs</read_first>
  <action>新增 `src-tauri/src/analysis/` 模块，并在 `contract.rs` 定义本 phase 的公开语义合同：`ANALYSIS_CONTRACT_VERSION`、`TEXT_ANALYSIS_VERSION`、`AnalysisSubtype`、`AnalysisStatus`、`AnalysisDiagnosticCode`、`AnalysisDiagnosticSeverity`、`AnalysisDiagnostic`、`AnalysisMetadata` tagged enum，以及 `AnalysisSnapshot`。`AnalysisMetadata` 必须为 URL、IP、Email、Color、Code、Command、Timestamp、Json、Markdown、Base64、PlainText 都预留稳定 variant 和字段，不要再沿用单个 `ContentMetadata` 结构硬塞所有 subtype。`AnalysisSnapshot` 必须包含 `contract_version`、`analysis_version`、`status`、`subtype`、`metadata`、`diagnostics`、`analyzed_at`，并提供诸如 `matched(...)` / `fallback_plain_text(...)` 这类构造 helper。`service.rs` 中新增 `TextAnalysisService`，公开单一入口，例如 `analyze(&self, text: &str) -> AnalysisSnapshot`，内部暂时可以 adapter 当前 `ContentDetector`，但本计划不要把 authority 写回 `clipboard_entries`，也不要在 `clipboard_entries` 上继续追加新的语义权威字段。最后在 `src-tauri/src/lib.rs` 注册 `analysis` 模块与后续测试模块。</action>
  <acceptance_criteria>
    - `rg -n "ANALYSIS_CONTRACT_VERSION|TEXT_ANALYSIS_VERSION|AnalysisSnapshot|AnalysisStatus|AnalysisDiagnostic|AnalysisMetadata" src-tauri/src/analysis/contract.rs` 有匹配结果。
    - `rg -n "pub struct TextAnalysisService|fn analyze\\(" src-tauri/src/analysis/service.rs` 有匹配结果。
    - `rg -n "mod analysis|mod analysis_contract_tests" src-tauri/src/lib.rs` 有匹配结果。
    - `rg -n "entry_analysis" src-tauri/src/analysis/contract.rs src-tauri/src/analysis/service.rs` 无匹配结果，本计划只建立 contract，不提前接 schema。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test --no-run</automated>
  </verify>
  <done>Phase 2 的语义合同已经在 Rust 侧落地，后续所有执行计划都可以直接围绕 `AnalysisSnapshot` 继续实现，而不是重新发明 subtype / metadata 形状。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 contract tests 固定 subtype corpus、precedence 和 fallback serialization</name>
  <files>src-tauri/src/analysis/service.rs, src-tauri/src/analysis_contract_tests.rs, src-tauri/src/lib.rs</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md, src-tauri/src/analysis/contract.rs, src-tauri/src/analysis/service.rs, src-tauri/src/clipboard/content_detector.rs</read_first>
  <behavior>
    - Test 1: `TextAnalysisService` 对 URL、JSON、code、command、color、markdown、email、IP、timestamp、base64、plain_text 返回稳定 subtype。
    - Test 2: 歧义输入的 precedence 被固定，例如 JSON 优先于 URL 片段、markdown 优先于裸 URL、命令不会被普通 code 盖掉。
    - Test 3: fallback snapshot 的 JSON 序列化会保留 `status`、`subtype`、`analysis_version`、`contract_version` 和 diagnostics，而不是丢失失败上下文。
  </behavior>
  <action>新增 `src-tauri/src/analysis_contract_tests.rs`，只通过 `TextAnalysisService` 作为断言入口，不直接在测试里依赖 `ContentDetector::detect()` 的 tuple 结果。建立一个固定 corpus，至少覆盖 `DETE-01` 要求的 11 个 subtype，以及 3-5 个 precedence 冲突样本。测试名固定为 `test_text_analysis_contract_preserves_supported_subtypes` 与 `test_text_analysis_contract_failure_fallback_serializes_diagnostics`，并补一个面向歧义输入的 precedence 测试。若当前 detector 无法一次性给出所有 typed metadata，也要先锁定公开 variant 和字段名，禁止在后续 plan 中再改 variant 命名或 status 语义。</action>
  <acceptance_criteria>
    - `rg -n "test_text_analysis_contract_preserves_supported_subtypes|test_text_analysis_contract_failure_fallback_serializes_diagnostics" src-tauri/src/analysis_contract_tests.rs` 有匹配结果。
    - `rg -n "markdown|json|command|base64|ip_address|plain_text" src-tauri/src/analysis_contract_tests.rs` 有匹配结果，证明 corpus 覆盖关键 subtype。
    - `rg -n "status|analysis_version|contract_version|diagnostics" src-tauri/src/analysis_contract_tests.rs src-tauri/src/analysis/contract.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_text_analysis_contract_preserves_supported_subtypes -- --nocapture && cargo test test_text_analysis_contract_failure_fallback_serializes_diagnostics -- --nocapture</automated>
  </verify>
  <done>`AnalysisSnapshot` 的 subtype surface、precedence 和 fallback 序列化合同已经被测试锁死，后续实现只能在这个 contract 内演进。</done>
</task>

</tasks>

<verification>
先执行 `cd src-tauri && cargo test --no-run`，确认 `analysis` 模块和测试注册不会破坏现有编译；再执行 contract tests，确认 subtype corpus 与 fallback serialization 已固定。
</verification>

<success_criteria>
Rust authoritative analysis contract 已存在且可测试，Phase 2 后续计划都可以围绕 `AnalysisSnapshot`、version 常量和统一 service 入口继续实现。
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-contracts-versioned-detection/02-01-SUMMARY.md`
</output>
