---
phase: 02-analysis-contracts-versioned-detection
plan: 06
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - src/lib/preview/previewDescriptor.ts
  - src/stores/clipboardStore.ts
  - src/components/DetailView/scene/AlternateViews.tsx
  - src/components/DetailView/DetailPreviewContract.test.tsx
  - src/components/DetailView/scene/AlternateViews.test.tsx
autonomous: true
requirements:
  - DETE-02
  - DETE-04
gap_closure: true
must_haves:
  truths:
    - 'URL 条目在 detail 中始终以 `url_card` 作为主视图，不会再被远端 `previewKind=json` 或其他 resolved 内容抢占。'
    - 'URL 解析得到的 JSON、文本或媒体内容只作为备用视图暴露，不会反向篡改 URL 本身的语义。'
    - 'URL 条目不会再把原始 URL 字符串塞进 JSON renderer，导致“无效 JSON”假回退。'
  artifacts:
    - path: 'src/lib/preview/previewDescriptor.ts'
      provides: 'URL-first primaryKind 与 resolved alternate views contract'
    - path: 'src/stores/clipboardStore.ts'
      provides: 'URL preview resolution 只返回真实 resolved payload，不再预填 raw URL 到 textContent'
    - path: 'src/components/DetailView/scene/AlternateViews.tsx'
      provides: 'URL resolved image/audio/video/json/text alternate renderer'
    - path: 'src/components/DetailView/DetailPreviewContract.test.tsx'
      provides: 'URL + remote json/text/media regression contract tests'
  key_links:
    - from: 'src/stores/clipboardStore.ts'
      to: 'src/lib/preview/previewDescriptor.ts'
      via: 'resolveEntryPreview only supplies actual resolved URL payload'
      pattern: 'resolveEntryPreview'
    - from: 'src/lib/preview/previewDescriptor.ts'
      to: 'src/components/DetailView/DetailPreviewContract.test.tsx'
      via: 'URL primaryKind and alternate views contract'
      pattern: 'buildPreviewDescriptor'
    - from: 'src/components/DetailView/scene/AlternateViews.tsx'
      to: 'src/components/DetailView/scene/AlternateViews.test.tsx'
      via: 'resolved media/text/json alternate rendering'
      pattern: 'renderView'
---

<objective>
闭合 UAT Test 2 的 URL detail 回归：URL 条目必须先展示 URL 自身结构，远端 resolved 内容只能作为备用视图出现。

Purpose: Phase 02 已把分析语义收回 Rust，但 detail preview 仍把“远端资源是什么”放在“条目本身是 URL”之前，导致 URL 主视图被 JSON/树形视图误抢。这个计划只修正 URL 语义合同，不扩张到 Phase 3 的整套 preview 重构。
Output: URL-first 的 descriptor/store 合同、resolved alternate views 渲染支持，以及对应的 URL 回归测试。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-UAT.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-04-SUMMARY.md
@.planning/debug/url-detail-renders-json-tree.md
@src/types/clipboard.ts
@src/lib/preview/previewDescriptor.ts
@src/stores/clipboardStore.ts
@src/components/DetailView/scene/AlternateViews.tsx
@src/components/DetailView/DetailPreviewContract.test.tsx

<interfaces>
关键预览类型：

```ts
export type PreviewKind =
  | 'plain_text'
  | 'code'
  | 'markdown'
  | 'json'
  | 'image'
  | 'audio'
  | 'video'
  | 'url_card'
  | 'file_card';

export interface PreviewAlternateView {
  key: string;
  label: string;
  kind: PreviewKind | 'raw';
  payload: unknown;
}

export interface ResolvedPreviewData {
  textContent?: string;
  jsonContent?: unknown;
  imageUrl?: string;
  audioUrl?: string;
  videoUrl?: string;
  url?: {
    previewKind?: PreviewKind;
    error?: string;
  };
}
```

当前错误合同：

```ts
if (subType === 'url') {
  if (hasResolvedJsonContent(resolvedData) || resolvedKind === 'json') {
    return 'json';
  }
}
```

```ts
if ((contentType.includes('text') || contentType.includes('string')) && entry.content_data) {
  resolved.textContent = entry.content_data;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 URL 条目的主视图合同收口回 url_card，并移除 raw URL 到 JSON fallback 的错误桥接</name>
  <files>src/lib/preview/previewDescriptor.ts, src/stores/clipboardStore.ts, src/components/DetailView/DetailPreviewContract.test.tsx</files>
  <behavior>
    - Test 1: `subType='url'` 且 `resolved.url.previewKind='json'` 时，`primaryKind` 仍为 `url_card`，同时保留 `resolved-json` 或 `resolved-text` 备用视图。
    - Test 2: `subType='url'` 且拿到远端 image/audio/video 时，descriptor 暴露对应 `resolved-*` 备用视图，但主视图仍保持 `url_card`。
    - Test 3: URL 条目没有真实 resolved JSON 数据时，不会再因为预填 `textContent=entry.content_data` 而触发“无效 JSON”假回退。
  </behavior>
  <action>修改 `previewDescriptor.ts` 的 URL 分支，按 UAT gap missing 收口成“URL first”语义：`subType === 'url'` 时主视图固定为 `url_card`，远端 resolved JSON/文本/媒体只追加到 `alternateViews`，不要再因为 `previewKind` 或 `jsonContent` 把 `primaryKind` 提升为 `json` / `code` / `markdown` / `plain_text`。同步修改 `resolveEntryPreview()`：普通 text/json 仍可把 raw content 放入 `resolved.textContent` / `resolved.jsonContent`，但 URL 条目在调用 `resolveUrlPreview()` 之前禁止预填 `resolved.textContent = entry.content_data`，只保留真实 resolved payload。更新 `DetailPreviewContract.test.tsx`，把现有 “URL + previewKind=json => primaryKind=json” 和 image/video/audio URL 主预览断言改成 URL-first 合同，并新增 `url + remote json/text` 回归用例。不要改动 `entry.analysis` 权威来源，也不要在这个任务里引入新的远端抓取策略。</action>
  <verify>
    <automated>pnpm test -- src/components/DetailView/DetailPreviewContract.test.tsx</automated>
  </verify>
  <done>URL 条目的 detail contract 已恢复为 URL-first：主视图稳定是 `url_card`，远端 resolved 内容只作为备用视图，不再触发错误的 JSON 主渲染。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 URL resolved 备用视图真正可渲染，并补组件级媒体/文本回归测试</name>
  <files>src/components/DetailView/scene/AlternateViews.tsx, src/components/DetailView/scene/AlternateViews.test.tsx</files>
  <behavior>
    - Test 1: `AlternateViews` 可以渲染 URL 的 `resolved-json` / `resolved-text` 备用视图，而不是只退回统一文本块。
    - Test 2: `AlternateViews` 可以渲染 URL 的 `resolved-image` / `resolved-audio` / `resolved-video` 备用视图，避免把媒体地址当成纯文本展示。
    - Test 3: 现有多 tab 备用视图容器行为保持不变。
  </behavior>
  <action>扩展 `AlternateViews.tsx` 的 `renderView()`，支持 `image` / `audio` / `video` 这三类 `PreviewKind` 的备用视图渲染；JSON/text 保持现有 renderer 路径。渲染 URL resolved 媒体时使用最小原生预览元素即可，不要在这里重建 `PrimaryPreviewRenderer` 或新增复杂状态。更新 `AlternateViews.test.tsx`，补 `resolved-image` 和 `resolved-json` 等组件级回归，确保 URL-first 合同下的备用视图实际可见，而不是只在 descriptor 上存在 key。</action>
  <verify>
    <automated>pnpm test -- src/components/DetailView/scene/AlternateViews.test.tsx</automated>
  </verify>
  <done>URL 条目的 resolved JSON/文本/媒体备用视图已经具备真实渲染能力，Task 1 产出的 alternate keys 不会停留在纸面合同。</done>
</task>

</tasks>

<verification>
先用 descriptor contract tests 锁死 URL-first 语义，再用 AlternateViews 组件测试确认 resolved JSON/文本/媒体备用视图真的能渲染。
</verification>

<success_criteria>
用户复制普通 HTTPS URL 时，detail 主区域稳定展示 URL 卡片和结构信息；即使远端资源被识别成 JSON 或媒体，也只作为备用视图出现，不再看到“JSON/树形视图 + 无效 JSON”。
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-contracts-versioned-detection/02-06-SUMMARY.md`
</output>
