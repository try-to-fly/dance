---
phase: 02-analysis-contracts-versioned-detection
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src-tauri/src/analysis/mod.rs
  - src-tauri/src/analysis/repository.rs
  - src-tauri/src/database/mod.rs
  - src-tauri/src/models/mod.rs
  - src-tauri/src/state.rs
  - src/types/clipboard.ts
autonomous: true
requirements:
  - DETE-02
  - DETE-03
  - DETE-04
must_haves:
  truths:
    - '`entry_analysis` companion table 成为 analysis 的持久化权威，`clipboard_entries` 继续保存 raw clipboard payload 和 capture 生命周期字段。'
    - '历史读取路径可以 join authoritative analysis，并把 `analysis` 结构、兼容 `content_subtype` 和兼容 `metadata` 一起返回给前端。'
    - '缺失 companion row 的老数据仍可回退到 legacy `content_subtype` / `metadata`，直到 Phase 2 后续计划完成 backfill 和 rebuild。'
  artifacts:
    - path: 'src-tauri/src/database/mod.rs'
      provides: '`entry_analysis` schema、JSON-valid 约束、索引和迁移'
    - path: 'src-tauri/src/analysis/repository.rs'
      provides: 'analysis UPSERT、join row 读取和 stale selector helper'
    - path: 'src-tauri/src/models/mod.rs'
      provides: '前后端共享的 `ClipboardEntry.analysis` / analysis DTO'
    - path: 'src/types/clipboard.ts'
      provides: 'TypeScript 侧 analysis snapshot、typed metadata 与 diagnostics 类型'
  key_links:
    - from: 'src-tauri/src/database/mod.rs'
      to: 'src-tauri/src/analysis/repository.rs'
      via: 'entry_analysis migration and SQL helper usage'
      pattern: 'entry_analysis'
    - from: 'src-tauri/src/state.rs'
      to: 'src-tauri/src/models/mod.rs'
      via: 'history join row mapped into ClipboardEntry DTO'
      pattern: 'analysis'
    - from: 'src/types/clipboard.ts'
      to: 'src-tauri/src/models/mod.rs'
      via: 'Tauri serialization contract'
      pattern: 'analysis'
---

<objective>
把 `AnalysisSnapshot` 接到 companion table 和共享读模型上：落 schema、落 repository helper、落 `ClipboardEntry.analysis`，但仍保留对 legacy row 的兼容回退。

Purpose: 这是 `entry_analysis` 方案真正成立的基础层，解决 typed metadata 如何入库、如何随历史读取返回，以及为何不用继续把语义权威塞回 `clipboard_entries`。
Output: `entry_analysis` schema、repository helper、`ClipboardEntry.analysis` DTO、`get_clipboard_history()` joined read model，以及对应的 TypeScript 类型。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md
@src-tauri/src/analysis/contract.rs
@src-tauri/src/database/mod.rs
@src-tauri/src/models/mod.rs
@src-tauri/src/state.rs
@src/types/clipboard.ts

<interfaces>
当前 history 读取仍是直接扫 raw table：

```rust
pub async fn get_clipboard_history(
    &self,
    limit: Option<i32>,
    offset: Option<i32>,
    search: Option<String>,
) -> Result<Vec<ClipboardEntry>> {
    sqlx::query_as::<_, ClipboardEntry>(
        "SELECT * FROM clipboard_entries ORDER BY created_at DESC LIMIT ? OFFSET ?",
    )
}
```

当前前端共享类型还没有 analysis 对象：

```ts
export interface ClipboardEntry {
  content_subtype?: ContentSubType | string | null;
  metadata?: string | null;
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 落 `entry_analysis` schema、repository helper 和 version-aware SQL 约束</name>
  <files>src-tauri/src/analysis/mod.rs, src-tauri/src/analysis/repository.rs, src-tauri/src/database/mod.rs</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md, src-tauri/src/database/mod.rs, src-tauri/src/analysis/contract.rs</read_first>
  <action>新增 `src-tauri/src/analysis/repository.rs`，把 analysis 相关 SQL 从 `database/mod.rs` 中抽离为 companion-table helper。`Database::init()` 的 migration 必须创建 `entry_analysis`，列至少包含 `entry_id`、`content_hash`、`contract_version`、`analysis_version`、`status`、`subtype`、`metadata_json`、`diagnostics_json`、`analyzed_at`；`entry_id` 通过外键指向 `clipboard_entries(id)` 并 `ON DELETE CASCADE`；`metadata_json` 与 `diagnostics_json` 用 SQLite `json_valid(...)` 做约束；为 `content_hash`、`analysis_version` / `contract_version` 建必要索引。repository 中同时提供三类 helper：`upsert_entry_analysis(...)`、`load_entry_analysis_for_history(...)`、`list_stale_entry_ids(...)`；这里先把 stale selector 做成 helper，为 `DETE-03` 铺路，但本计划不要接 Preferences UI。明确写死本 phase 方案：不要在 `clipboard_entries` 上新增新的语义权威列，不要把 `entry_analysis` 简化成“再加几个 subtype/metadata 字段”。</action>
  <acceptance_criteria>
    - `rg -n "CREATE TABLE IF NOT EXISTS entry_analysis|json_valid|FOREIGN KEY|ON DELETE CASCADE" src-tauri/src/database/mod.rs` 有匹配结果。
    - `rg -n "upsert_entry_analysis|load_entry_analysis_for_history|list_stale_entry_ids" src-tauri/src/analysis/repository.rs` 有匹配结果。
    - `rg -n "contract_version|analysis_version|diagnostics_json|metadata_json" src-tauri/src/database/mod.rs src-tauri/src/analysis/repository.rs` 有匹配结果。
    - `rg -n "ALTER TABLE clipboard_entries ADD COLUMN analysis_" src-tauri/src/database/mod.rs` 无匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_database_reads_analysis_columns_after_migration -- --nocapture && cargo test test_database_upserts_entry_analysis_rows -- --nocapture</automated>
  </verify>
  <done>`entry_analysis` companion table 已经存在且有可复用的 repository helper；version-aware persistence 的 SQL 基础已经闭合。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展共享 `ClipboardEntry` / TypeScript 类型，并把 history 读模型切到 authoritative analysis first</name>
  <files>src-tauri/src/models/mod.rs, src-tauri/src/state.rs, src/types/clipboard.ts, src-tauri/src/analysis/repository.rs</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md, src-tauri/src/models/mod.rs, src-tauri/src/state.rs, src/types/clipboard.ts, src-tauri/src/analysis/repository.rs</read_first>
  <behavior>
    - Test 1: 新建的 `ClipboardEntry` 默认 `analysis` 为空，不会破坏老数据或非 text entry。
    - Test 2: history query 在有 companion row 时优先返回 authoritative `analysis`，并把兼容字段 `content_subtype` / `metadata` 从 authoritative row 反映出来。
    - Test 3: history query 在没有 companion row 时仍回退 legacy 字段，前端不会因为 backfill 尚未完成而拿到空白详情。
  </behavior>
  <action>更新 `src-tauri/src/models/mod.rs` 和 `src/types/clipboard.ts`，新增共享 `EntryAnalysisSnapshot`、typed metadata 和 diagnostics 类型，并把它们挂到 `ClipboardEntry.analysis` 上。`src-tauri/src/state.rs` 的 `get_clipboard_history()` 不再 `SELECT * FROM clipboard_entries` 直出，而是通过 repository join 出 `analysis`，然后按这个规则组装 DTO：1）若 companion row 存在，则 `analysis` 为 authoritative snapshot，同时兼容字段 `content_subtype` / `metadata` 回写自 authoritative row，供当前前端最小改动消费；2）若 companion row 缺失，则 `analysis = null`，保留 legacy `content_subtype` / `metadata`；3）任何 image / file / non-text entry 都不强行构造 text analysis。保持命令签名 `get_clipboard_history()` 不变，不要在本计划引入新的前端 API surface。</action>
  <acceptance_criteria>
    - `rg -n "analysis\\?:|analysis: EntryAnalysisSnapshot|EntryAnalysisSnapshot" src/types/clipboard.ts src-tauri/src/models/mod.rs` 有匹配结果。
    - `rg -n "content_subtype.*analysis|metadata.*analysis|analysis.*legacy" src-tauri/src/state.rs src-tauri/src/analysis/repository.rs` 有匹配结果。
    - `rg -n "test_clipboard_entry_analysis_fields_default_to_none|test_database_merges_analysis_fields|test_database_reads_analysis_columns_after_migration" src-tauri/src/models/mod.rs src-tauri/src/database/mod.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_clipboard_entry_analysis_fields_default_to_none -- --nocapture && cargo test test_database_merges_analysis_fields -- --nocapture</automated>
  </verify>
  <done>前后端共享 DTO 已经具备 `analysis` 结构，history read model 也已改成 authoritative analysis first、legacy fallback second。</done>
</task>

</tasks>

<verification>
先验证 migration 和 repository SQL，再验证 models/state 的 joined read model。通过后，`entry_analysis` 方案已经拥有可读取、可兼容的最小闭环。
</verification>

<success_criteria>
`entry_analysis` companion table 已经成为 analysis 的持久化权威，Rust 和 TypeScript 共享类型都能表达 `AnalysisSnapshot`，并且历史读取对老数据保持兼容回退。
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-contracts-versioned-detection/02-02-SUMMARY.md`
</output>
