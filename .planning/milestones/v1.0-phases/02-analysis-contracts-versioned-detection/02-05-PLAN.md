---
phase: 02-analysis-contracts-versioned-detection
plan: 05
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src-tauri/src/analysis/mod.rs
  - src-tauri/src/analysis/rebuild.rs
  - src-tauri/src/state.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/integration_tests.rs
  - src/components/Preferences/PreferencesModal.tsx
  - src/components/Preferences/PreferencesModal.test.tsx
autonomous: true
requirements:
  - DETE-03
  - DETE-04
must_haves:
  truths:
    - '已有历史可以批量重跑 analysis，而不需要用户重新复制内容。'
    - 'rebuild 只更新 companion analysis，不会改写 raw content、entry id、copy_count、favorites 或 file path。'
    - 'Preferences 有一个最小可见入口触发 rebuild，并在成功后刷新历史与缓存统计。'
  artifacts:
    - path: 'src-tauri/src/analysis/rebuild.rs'
      provides: 'batched reanalysis service 与 stale selector 消费逻辑'
    - path: 'src-tauri/src/commands.rs'
      provides: 'Tauri rebuild command 与结果 DTO'
    - path: 'src/components/Preferences/PreferencesModal.tsx'
      provides: '用户可触发的 rebuild action 与最小结果反馈'
    - path: 'src-tauri/src/integration_tests.rs'
      provides: 'existing history reanalysis integration tests'
  key_links:
    - from: 'src-tauri/src/analysis/rebuild.rs'
      to: 'src-tauri/src/analysis/repository.rs'
      via: 'select stale rows and upsert fresh snapshots'
      pattern: 'list_stale_entry_ids'
    - from: 'src-tauri/src/commands.rs'
      to: 'src-tauri/src/state.rs'
      via: 'rebuild_entry_analysis command'
      pattern: 'rebuild_entry_analysis'
    - from: 'src/components/Preferences/PreferencesModal.tsx'
      to: 'src-tauri/src/commands.rs'
      via: "invoke('rebuild_entry_analysis')"
      pattern: 'rebuild_entry_analysis'
---

<objective>
为已有历史建立 version-aware reanalysis：Rust 端可批量重建 companion rows，前端只提供一个最小 Preferences 触发入口和刷新动作。

Purpose: 这是 `DETE-03` 的直接闭环，确保 detector/contract 升级后，旧历史不必靠“重新复制一次”才能获得新分析结果。
Output: batched reanalysis service、Tauri rebuild command、integration tests，以及 Preferences 里的最小 rebuild 按钮。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md
@.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md
@src-tauri/src/analysis/contract.rs
@src-tauri/src/analysis/repository.rs
@src-tauri/src/state.rs
@src-tauri/src/commands.rs
@src/components/Preferences/PreferencesModal.tsx

<interfaces>
当前命令层还没有 rebuild 入口：

```rust
#[tauri::command]
pub async fn get_clipboard_history(...) -> Result<Vec<ClipboardEntry>, String>
```

Preferences 的 system/cache 区域已经具备加载统计的 UI 容器，可复用为 rebuild 入口：

```tsx
const { cacheStats, loadCacheStatistics } = useConfigStore();
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 batch reanalysis service、state/command 入口和 integration tests</name>
  <files>src-tauri/src/analysis/mod.rs, src-tauri/src/analysis/rebuild.rs, src-tauri/src/state.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs, src-tauri/src/integration_tests.rs</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-RESEARCH.md, src-tauri/src/analysis/repository.rs, src-tauri/src/state.rs, src-tauri/src/commands.rs, src-tauri/src/integration_tests.rs, src-tauri/src/lib.rs</read_first>
  <behavior>
    - Test 1: rebuild 会选择缺失 companion row 或 `analysis_version` / `contract_version` 过期的 text entries，批量重算 analysis。
    - Test 2: rebuild 只更新 `entry_analysis`，不会改写 raw entry 的 `id`、`content_data`、`copy_count`、`is_favorite`、`file_path`。
    - Test 3: non-text entries 会被跳过，结果统计会明确区分 updated / skipped / failed。
  </behavior>
  <action>新增 `src-tauri/src/analysis/rebuild.rs`，封装 batch rebuild 逻辑，复用 `TextAnalysisService` 与 `list_stale_entry_ids(...)`。rebuild 结果定义为最小 DTO，例如 `RebuildEntryAnalysisResult { scanned, updated, skipped, failed }`。`AppState` 新增 `rebuild_entry_analysis(batch_size: Option<usize>)`，`commands.rs` 暴露 `#[tauri::command] rebuild_entry_analysis(...)`，并在 `lib.rs` 注册。执行策略必须是：只针对 text entry，按批次读取 stale / missing rows，重算 snapshot 后 UPSERT 到 `entry_analysis`；不要删除 raw rows，不要动 `clipboard_entries` 的生命周期字段。集成测试名固定包含 `test_rebuild_text_analysis_updates_existing_history` 与 `test_rebuild_text_analysis_skips_non_text_entries`。</action>
  <acceptance_criteria>
    - `rg -n "RebuildEntryAnalysisResult|rebuild_entry_analysis|batch_size|updated|skipped|failed" src-tauri/src/analysis/rebuild.rs src-tauri/src/state.rs src-tauri/src/commands.rs` 有匹配结果。
    - `rg -n "rebuild_entry_analysis" src-tauri/src/lib.rs` 有匹配结果。
    - `rg -n "test_rebuild_text_analysis_updates_existing_history|test_rebuild_text_analysis_skips_non_text_entries" src-tauri/src/integration_tests.rs` 有匹配结果。
  </acceptance_criteria>
  <verify>
    <automated>cd src-tauri && cargo test test_rebuild_text_analysis_updates_existing_history -- --nocapture && cargo test test_rebuild_text_analysis_skips_non_text_entries -- --nocapture</automated>
  </verify>
  <done>Rust 侧已经可以对历史数据执行 version-aware reanalysis，并通过命令层暴露给前端触发。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 Preferences 的 system/cache 区域接一个最小 rebuild action，并刷新历史与统计</name>
  <files>src/components/Preferences/PreferencesModal.tsx, src/components/Preferences/PreferencesModal.test.tsx</files>
  <read_first>.planning/phases/02-analysis-contracts-versioned-detection/02-VALIDATION.md, src/components/Preferences/PreferencesModal.tsx, src/components/DetailView/DetailView.tsx, src/stores/clipboardStore.ts</read_first>
  <behavior>
    - Test 1: 点击 rebuild 按钮会调用 `invoke('rebuild_entry_analysis')`，并展示 loading / success / error 的最小反馈。
    - Test 2: rebuild 成功后会刷新 cache statistics，并触发历史与 preview cache 刷新。
    - Test 3: 这个入口只做本地 rebuild，不引入搜索、同步、分享、团队协作或后台常驻任务。
  </behavior>
  <action>在 `PreferencesModal.tsx` 的 system/cache 区域加入一个最小 rebuild action，例如按钮 + 结果摘要，不做新的复杂设置页。按钮点击后直接 `invoke('rebuild_entry_analysis', { batchSize })`，成功时调用已有 `loadCacheStatistics()`，并通过 `useClipboardStore.getState().invalidatePreview()` 与 `useClipboardStore.getState().fetchHistory()` 刷新前端数据；失败时显示现有风格的错误反馈。新增 `PreferencesModal.test.tsx`，mock Tauri `invoke` 与 store 静态入口，锁定命令调用和刷新副作用。不要引入新 store、不要把 rebuild 扩展成自动后台任务。</action>
  <acceptance_criteria>
    - `rg -n "rebuild_entry_analysis|fetchHistory|invalidatePreview|loadCacheStatistics" src/components/Preferences/PreferencesModal.tsx` 有匹配结果。
    - `rg -n "invoke\\('rebuild_entry_analysis'" src/components/Preferences/PreferencesModal.tsx src/components/Preferences/PreferencesModal.test.tsx` 有匹配结果。
    - `rg -n "search|sync|share|team" src/components/Preferences/PreferencesModal.tsx` 无新增与本计划无关的功能入口。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/Preferences/PreferencesModal.test.tsx</automated>
  </verify>
  <done>用户已经可以从 Preferences 手动触发历史 reanalysis，并看到最小反馈；重建后历史与统计也会刷新。</done>
</task>

</tasks>

<verification>
先跑 Rust integration tests，确认 rebuild 只更新 companion analysis；再跑 PreferencesModal 测试，确认前端触发和刷新副作用已经闭合。
</verification>

<success_criteria>
Phase 2 的历史 reanalysis 闭环已成立：旧历史可以在本地批量重算 analysis，且用户能从 Preferences 直接触发，而无需重新复制内容。
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-contracts-versioned-detection/02-05-SUMMARY.md`
</output>
