---
phase: 05-rebuild-safety-release-gates
plan: 01
type: execute
wave: 1
depends_on:
  - 02-05
  - 04-01
files_modified:
  - src-tauri/src/analysis/rebuild.rs
  - src-tauri/src/retrieval/mod.rs
  - src/components/Preferences/PreferencesModal.tsx
  - src/components/Preferences/PreferencesModal.test.tsx
  - .github/workflows/test-build.yml
  - .github/workflows/release.yml
autonomous: true
requirements:
  - RELY-01
  - RELY-02
must_haves:
  truths:
    - '既有 `rebuild_entry_analysis` 入口会同时重建 search documents/index，不需要额外的新命令。'
    - 'rebuild 动作对已有历史保持 non-destructive：刷新 analysis 与检索索引，但不清空历史条目。'
    - 'Preferences rebuild 结果会明确反馈 search reindex 成功和失败计数。'
    - 'test-build 与 release workflow 在所有打包步骤前都会先执行 `pnpm test`、`pnpm build`、`cargo test`。'
  artifacts:
    - path: src-tauri/src/analysis/rebuild.rs
      provides: 'analysis rebuild + search rebuild 串联结果'
    - path: src/components/Preferences/PreferencesModal.tsx
      provides: 'search rebuild 结果反馈 UI'
    - path: .github/workflows/test-build.yml
      provides: 'pre-package validation gate for test builds'
    - path: .github/workflows/release.yml
      provides: 'pre-package validation gate for release builds'
  key_links:
    - from: src-tauri/src/analysis/rebuild.rs
      to: src-tauri/src/retrieval/mod.rs
      via: rebuild_search_documents
      pattern: rebuild_search_documents
    - from: src/components/Preferences/PreferencesModal.tsx
      to: src-tauri/src/commands.rs
      via: rebuild_entry_analysis result rendering
      pattern: rebuild_entry_analysis
    - from: .github/workflows/test-build.yml
      to: .github/workflows/release.yml
      via: matching validation gates before package
      pattern: cargo test
---

<objective>
闭合 Phase 05 的 rebuild safety 与 release gate：确保已有 history 在 analysis/search 升级后仍可重建可用，并让所有打包工作流都先经过自动化验证。

Purpose: 这是 RELY-01 与 RELY-02 的交付面。没有这一步，search documents 会在升级后变 stale，正式打包流程也可能绕过本地回归门禁。
Output: rebuild-safe maintenance path、search rebuild 结果反馈，以及 pre-package validation gates。
</objective>

<execution_context>
@/Users/smile/.codex/get-shit-done/workflows/execute-plan.md
@/Users/smile/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-rebuild-safety-release-gates/05-CONTEXT.md
@.planning/phases/05-rebuild-safety-release-gates/05-RESEARCH.md
@src-tauri/src/analysis/rebuild.rs
@src-tauri/src/retrieval/mod.rs
@src/components/Preferences/PreferencesModal.tsx
@.github/workflows/test-build.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 search rebuild 串进现有 analysis rebuild，并把结果反馈到 Preferences</name>
  <files>src-tauri/src/analysis/rebuild.rs, src-tauri/src/retrieval/mod.rs, src/components/Preferences/PreferencesModal.tsx, src/components/Preferences/PreferencesModal.test.tsx</files>
  <read_first>.planning/phases/05-rebuild-safety-release-gates/05-CONTEXT.md, src-tauri/src/analysis/rebuild.rs, src-tauri/src/retrieval/mod.rs, src/components/Preferences/PreferencesModal.tsx</read_first>
  <behavior>
    - Test 1: rebuild 结果对象新增 search reindex 成功/失败计数。
    - Test 2: `rebuild_entry_analysis` 成功后，Preferences 会显示 analysis 与 search 的双重统计，而不是只显示 analysis。
    - Test 3: search rebuild 复用现有 search document 逻辑，不新增独立 command。
  </behavior>
  <action>在 `EntryAnalysisRebuilder::rebuild()` 中调用 `rebuild_search_documents(pool)`，把 `search_reindexed` 与 `search_failed` 纳入结果对象；在 Preferences modal 与测试中同步展示和断言这两个字段，保持 rebuild 入口不变。</action>
  <acceptance_criteria>
    - `rg -n "search_reindexed|search_failed|rebuild_search_documents" src-tauri/src/analysis/rebuild.rs src/components/Preferences/PreferencesModal.tsx src/components/Preferences/PreferencesModal.test.tsx` 有匹配结果。
    - `rg -n "rebuild_entry_analysis" src/components/Preferences/PreferencesModal.tsx src-tauri/src/commands.rs` 有匹配结果，且没有新增 search rebuild command。 
  </acceptance_criteria>
  <verify>
    <automated>pnpm test && cargo test</automated>
  </verify>
  <done>用户通过现有 rebuild 入口即可同时刷新 analysis 与 search documents，且 UI 能看见完整结果。</done>
</task>

<task type="auto">
  <name>Task 2: 为 release 与 test-build 添加打包前 validation gates</name>
  <files>.github/workflows/test-build.yml, .github/workflows/release.yml</files>
  <read_first>.planning/phases/05-rebuild-safety-release-gates/05-RESEARCH.md, .github/workflows/test-build.yml, .github/workflows/release.yml</read_first>
  <action>在 Windows/macOS test-build job 与 release matrix job 中，在任何 Tauri build 之前统一加入 `pnpm test`、`pnpm build`、`cargo test`。保持现有平台分支和签名配置不变，只补 gate，不扩张为新的部署流程。</action>
  <acceptance_criteria>
    - `rg -n "pnpm test|pnpm build|cargo test" .github/workflows/test-build.yml .github/workflows/release.yml` 有匹配结果。
    - workflow 中这些 step 出现在 Tauri build step 之前。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test && pnpm build && cd src-tauri && cargo test</automated>
  </verify>
  <done>所有打包路径都先经过前端测试、前端构建和 Rust 测试，回归不会被直接打进产物。</done>
</task>

</tasks>

<verification>
运行 `pnpm test`、`pnpm build`、`cargo test`，确认 rebuild feedback 与 workflow gate 改动没有破坏现有桌面应用链路。
</verification>

<success_criteria>
Phase 05 完成后，用户无需清空历史即可在升级后重建 analysis 与 search index；团队在打包前也会先经过自动化 gate，再进入 release artifacts 生成。
</success_criteria>

<output>
After completion, create `.planning/phases/05-rebuild-safety-release-gates/05-01-SUMMARY.md`
</output>
